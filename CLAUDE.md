# LETA Design System — Context for Claude

This file is loaded into context for every Claude Code session in this repo. Read [PLAN.md](PLAN.md) for the full roadmap.

## Project goal

Build the LETA design system in code, **mirroring the Figma file as the source of truth**, plus an interactive product playground. Engineers consume the React components; designers iterate in Figma. The Figma file key is `Kxbgc2KoJSmTxvSV3PwNEu`.

## Tech stack

- React 19 + TypeScript
- Tailwind CSS — preset built from tokens, not hand-written
- pnpm workspaces + Turborepo
- Vite (apps), tsup (packages)

## Repo layout

```
packages/
  design-tokens/   # generated CSS vars, Tailwind preset, TS exports
  icons/           # @leta/icons — Material Symbols-backed registry
  components/      # @leta/components — React component library
  cli/             # @leta/cli — Figma → code token pipeline
apps/
  storybook/       # @leta/storybook — component docs
  playground/      # @leta/playground — product prototypes (Phase 3)
```

## Token pipeline

Figma variables → JSON snapshots in `packages/cli/snapshots/` → CLI generators → outputs in `packages/design-tokens/dist/` (`tokens.css`, `text-styles.css`, `tailwind.preset.cjs`) and `packages/design-tokens/src/generated/tokens.ts`.

Commands (run from repo root):
- `pnpm tokens:fetch` — pull fresh variables from Figma via WebSocket bridge (requires Figma desktop + Desktop Bridge plugin)
- `pnpm tokens:generate` — regenerate outputs from snapshots
- `pnpm tokens:check` — diff regenerated output vs committed (CI uses this)
- `pnpm tokens:sync` — fetch + generate

**Bridge port note**: `tokens:fetch` binds a WebSocket *server* on port `9226` (fallbacks `9227–9229`) and waits for the Figma Desktop Bridge plugin to dial in. The figma-console MCP runs the *same* kind of server.

**Why `pnpm tokens:fetch` (and `tokens:sync`) fails inside a Claude Code session — confirmed 2026-05-27:** The Desktop Bridge plugin holds **exactly one connection at a time**. If any Claude Code session has the figma-console MCP connected, the plugin is bonded to *it* (the plugin panel shows "MCP ready"), so the CLI's competing server never wins the plugin:
- ports `9226`/`9227` → `skip` (plugin never dials in; 8s timeout each)
- a later port may complete a stale WS handshake, but `EXECUTE_CODE` comes back in the MCP's envelope shape, not the CLI's expected `{id, result:"<json>"}` → fails with `"[object Object]" is not valid JSON` and file shows `"?"`.

This is **structural**: `tokens:fetch` (CLI bridge) and `figma_execute` (figma-console MCP) are mutually-exclusive consumers of one single-connection plugin. Closing the *previous* session doesn't help if the *current* one also has the MCP — it re-grabs the plugin instantly.

**Two ways to refresh tokens:**
1. **Plain terminal, no Claude session with the MCP** — quit all Claude Code sessions running figma-console MCP, then `pnpm tokens:sync` in a normal terminal. (Can't be driven by Claude, since Claude needs the MCP.)
2. **From inside a Claude session (the practical path)** — the session already owns the plugin via the MCP, so refresh snapshots by running the CLI's extraction snippets (`packages/cli/src/bridge/extract.ts`: `variableCollectionSnippet`, `textStylesSnippet`, `effectStylesSnippet`) through `figma_execute`, write each returned JSON string verbatim to `packages/cli/snapshots/<file>.json`, then run `pnpm tokens:generate` + `pnpm tokens:check`. The 5 collections are Brand/Alias/Mapped Colors (leading space in Figma)/Mapped Type/Mapped Sizes. This is exactly what the figma-design-system-sync skill's Phase 2a now documents.

## Per-component workflow

Every component goes through the 8-step workflow defined in [PLAN.md](PLAN.md#per-component-workflow):

1. **Behaviour spec** — read Figma description + annotations + variants. Synthesize: what it is, how it works, when to use, when NOT to use. Draft missing pieces. **Confirm with user before writing code.**
2. Property mapping (Figma variants → React props)
3. Implement in `packages/components/src/<Name>/`
4. Storybook story covering every variant
5. Lint: zero hardcoded colors/sizes
6. Visual diff against Figma at multiple breakpoints
7. **Parity check** with `figma_check_design_parity` — gate on **visual fidelity only**:
   - Zero `critical` discrepancies anywhere
   - Zero `major` discrepancies in `visual` / `spacing` / `typography` categories
   - Documented numeric / normalization artifacts (pill radius `896` vs `9999`, COMPONENT_SET chrome `134×80` vs `40×40`) explicitly allowed — note in the PLAN.md sign-off line
   - Aggregate score is **informational only**, not a gate. React-composition idioms cause `componentAPI` deductions that are paradigm choices, not visual gaps. Ceilings: simple atoms 90–96, composition-heavy components (Buttons) 60–70, components with COMPONENT_SET chrome 75–85.
8. Mark done in PLAN.md

Key rules:
- **Behaviour spec confirmation is non-negotiable** — never start implementation until the user has approved the spec.
- **Never assume a variant's structure** — component sets routinely vary *structurally* across variants (different layout direction, different child elements, different trailing content, present-but-hidden nodes). Analyze **every** variant individually: walk each one's tree and check node `visible` flags **and stroke/fill paint `visible` — not just stroke weight**. Do not extract one variant and extrapolate to the rest. (Learned 2026-06-04: Desktop Progress Indicator's Upload type is horizontal with a trailing %/check while Task & System Process are vertical with a helper-text line and no %; the Stepper count "underline" was a 1.5px stroke whose paint was `visible:false` in every state except Focus.)
- **Always VIEW the rendered variant image during the build, not just the structure tree** — download `figma_get_component_image` to a temp file and `Read` it. The structure tree hides visual patterns the code must reproduce — e.g. Pagination's page-range windowing (`1 2 3 … N`), exact active-cell styling, real glyph shapes. (Learned 2026-06-05: built Pagination's page range from the tree alone; the windowing didn't match Figma's `1 2 3 … 10` until the rendered image was viewed.)
- **Respect nested-instance property configuration** — when a component (molecule/organism/template/etc.) composes an *instance* of another component (an atom/molecule), the new component must reproduce the **exact property values that instance carries in Figma**, not the consumed component's React defaults. Read each nested instance's `componentProperties` from the Figma tree and pass them through explicitly; never assume the atom's default matches. (Learned 2026-06-08: Breadcrumbs' Plain Button instances are `Show Underline: false` in Figma — and the `Underline` node is `visible:false` — but the `Button` atom defaults `showUnderline = true`, so the crumbs shipped with an underline the design doesn't have until `showUnderline={false}` was passed.)
- **Visual parity is non-negotiable** — zero `critical`, zero `major` in `visual` / `spacing` / `typography` before checking off. Aggregate parity score is informational.
- **Figma Slots → composable child-injection props (never flatten, never hardcode)** — Figma natively supports **Slots** (`node.type === 'SLOT'`), used across the complex components (Modal Headers, Content Primitives, Content Cards, Cells, etc.) as flexible composition points. They are NON-NEGOTIABLE to detect and map correctly:
  1. **Detect** — during property-mapping, walk the component tree for **every** `type === 'SLOT'` node (in every variant). For each slot capture: its **name**, its **auto-layout** (`layoutMode`/`itemSpacing`/padding/`primaryAxisAlignItems`/`counterAxisAlignItems`/`layoutSizingHorizontal`/`layoutSizingVertical`), its **visibility**, and any **Preferred Instances**. List all detected slots in the behaviour spec presented to the user before building.
  2. **Map** — each named Figma slot → a `ReactNode` slot prop named from the slot's Figma name (e.g. "Secondary Content" → `secondaryLeading`/`secondaryTrailing` when the slot's auto-layout visibly splits the row; otherwise one prop). The slot's auto-layout becomes a thin wrapper `<div>` the component owns; the **contents are always injected by the caller**. Never hardcode slot contents into the base component. Items set to "Fill Container" in Figma → `width:100%`/`flex:1`.
  3. **Type** — if the slot declares Preferred Instances, document them in the prop's JSDoc as the permitted-components contract.
  4. **Visibility mirrors Figma** — keep an explicit `show<SlotName>` boolean toggle (so prototypes can reveal/animate the slot later), but its **default** mirrors the Figma slot's `visible` flag: visible slot → defaults `true` and reserves its row even when empty; hidden slot → defaults `false` but the prop still exists for prototype use. **Do NOT render invented dashed-outline placeholders** — if the slot is visible in Figma, render it with the real nested instances Figma shows; if hidden, render nothing (just reserve the prop). (Learned 2026-06-09: Modal Headers' "Secondary Content" SLOT `9200:38903` is a SPACE_BETWEEN row — Status Badges left, Header CTAs right — that ships visible; mapped to `secondaryLeading`/`secondaryTrailing` with `showSecondaryContent` defaulting `true`, default story mirrors Figma's Scheduled badge + 2 Dispatch buttons.)

## Figma MCP strategy

Use the Figma Dev MCP first; escalate to figma-console MCP for richer data, writes, or parity checks. Detail in [PLAN.md § Tooling Notes](PLAN.md#figma-access-strategy).

Common calls:
- `figma_get_component_for_development` — variants + token bindings + screenshot
- `figma_get_component` — description + property definitions
- `figma_get_annotations` — designer-authored specs
- `figma_check_design_parity` — required for component sign-off
- `figma_execute` — escape hatch for ad-hoc plugin API calls (resolving variable IDs, refreshing snapshots without the bridge)

### figma-console REST token lives in `~/.claude.json` (confirmed 2026-06-03)

REST-based calls (`figma_check_design_parity`, `figma_take_screenshot`, `figma_get_component_image`) authenticate with `FIGMA_ACCESS_TOKEN`. **Claude Code reads its MCP `env` from `~/.claude.json` (`mcpServers.figma-console.env.FIGMA_ACCESS_TOKEN`), NOT from `claude_desktop_config.json`** — those are separate files for separate apps (the latter feeds the Claude Desktop chat). A `403 {"err":"Token expired"}` despite a valid PAT almost always means the token was updated in the wrong file, or the live MCP server is running a stale env. The harness **caches MCP env at session start** and reuses it even when the server is killed/respawned mid-session — so after editing `~/.claude.json` a **full Claude Code session restart** is required (a mid-session `/mcp` reconnect or `kill` re-spawns with the cached old env). Verify the token directly with `curl -H "X-Figma-Token: $TOK" https://api.figma.com/v1/me` (200 = valid) and `ps eww <mcp-pid> | tr ' ' '\n' | grep FIGMA_ACCESS_TOKEN` to see which token the live server actually holds. Keep both config files in sync when rotating the PAT.

### Why the bridge sometimes prompts to publish (confirmed 2026-06-03)

The Desktop Bridge plugin does **not** silently edit components/variables — code inspection shows its only launch-time action is a read (`figma.loadAllPagesAsync()` to attach a `documentchange` *listener*); every document-writing call (`setProperties`, `setBoundVariable`, `editComponentProperty`, `componentPropertyDefinitions =`) is gated behind an explicit edit/generation tool. **Read-only sessions do not flip the file's modified flag** — verified 2026-06-03: a read-only session (status probes, `figma_execute` reads, screenshots) left the Library clean with no publish prompt. So every Publish prompt reflects a *real* change to a published asset. The catch: Figma counts **layout / organizational moves** (reparenting or reordering published components — e.g. the icon-frame category reorg) as publishable changes even when no property or variable changed — that's the usual "but I didn't change anything" surprise. Mitigations: batch edits and publish once; open the Publish dialog (it lists exactly what changed) and dismiss/publish accordingly; for purely experimental or organizational work, use a separate scratch file so the published Library stays stable.

## Storybook conventions

- Component stories live next to the component: `packages/components/src/<Name>/<Name>.stories.tsx`
- Title: `Atoms/<Name>` (or `Molecules/`, `Organisms/`, etc.)
- Stories cover every Figma variant + a `Catalog` story showing the full matrix
- Foundations stories live in `apps/storybook/src/foundations/` and auto-derive from the generated tokens map
- **CSS grid Catalog stories:** Every component instance that is a direct child of a CSS grid must have `style={{ justifySelf: 'start' }}` (or be wrapped in a `<div>`). Without it the component stretches to fill the grid cell instead of hugging its content. Apply to all future Catalog stories.
- **Flex column layout:** When building stories or layouts with `flexDirection: 'column'`, flex containers default to `align-items: stretch`. If children should hug their natural width, add `alignItems: 'flex-start'` to the wrapper.
- **Story-list confirmation is mandatory (added 2026-05-25):** Before creating a Storybook story file for any component, present the planned list of stories to the user via `AskUserQuestion` and wait for explicit approval. Allowed story kinds: (a) a Figma variant of the component, (b) a boolean property of the component, (c) the default state, (d) a single-component `Catalog` / `AllStates` matrix view. **Never invent multi-instance "showcase" scenarios** (filter groups, wizard flows, tab bars, delivery-context grids, removable lists, etc.) without prior approval — those are molecule-level compositions and live elsewhere in the design system.
- **Sidebar ordering:** Story navigation order in the sidebar is controlled by `apps/storybook/.storybook/preview.tsx` → `storySort.order`. The Atoms section currently forces ViewSwitcherTab → SegmentSwitcherTab → PageTab → WizardTab → Title to be adjacent, with everything else sorted alphabetically via `'*'`.

## Status

Done:
- Token pipeline + CI drift check
- **Token pipeline now extracts effect styles too** — `packages/cli/snapshots/effect-styles.json` is a sibling of the variable snapshots, generated alongside them. The CSS generator emits `--shadow-{red,blue,neutral-1,neutral-2,neutral-3}` and TS exports include matching `shadowRed`/`shadowBlue`/`shadowNeutral{1,2,3}` keys.
- Foundation runtime (`<LetaThemeProvider>`, font loading)
- Storybook bootstrap + all foundation stories (Colors, Typography, Padding, Spacing, Rounding, Stroke, Opacity)
- Icons package (`@leta/icons`) — **356 icon components → 254 semantic `IconName`s** backed by Material Symbols (incl. `Check-Circle` added 2026-05-24; `Check-Circle-Outline` renamed from `Check-Outline` 2026-05-24); **upgraded from `@material-symbols/svg-400` to `svg-500` on 2026-05-25** so icon strokes match Figma's heavier weight across every consuming component (Button, Badge, Chip, FeaturedIcon, MapIconType, Tag, Toggle, Checkbox, RadioButton). No API change — weight is baked into SVG path data. **Icon library audit + sync 2026-06-03:** in Figma, deduped 3 glyph-collision names (`Add`→`Add-Circle`, `Question`→`Help`, `Note`(outline)→`Note-Outline`), repaired 2 broken `Icon/Deactivate` frames (→ new `Icon/Deactivate` component + recovered `Hourglass-Outline`), moved Toggle/Checkbox/Radio Button out of the icon frame, reorganized the `LETA Icons` frame (`8063:7080`) into 18 labeled category rows, and normalized all remaining names to `Title-Case-With-Hyphens` (spaces/ampersand/typos: e.g. `VOIP Call`→`VOIP-Call`, `Trips & Routing`→`Trips-Routing`, `Lighbulb`→`Lightbulb`, `Cancel 2`→`Cancel-Circle`). Also re-synced every icon **instance-swap `preferredValues`** so all 48 icon slots across 27 component sets offer the full icon set. Code side: `packages/icons/snapshots/inventory.json` regenerated from Figma + `pnpm registry` rebuilt `src/generated/{registry.ts,icon-names.ts}` (0 unresolved glyphs; only `Circle-Medium` uses `overrides.json`). Figma library **published 2026-06-03**. **New `Open` icon added 2026-06-15** (`Icon/Open` `10391:39666`, key `212edbd90b29c620b9eed208c5b9e876eef0dc0a`, glyph `open-in-new-rounded` — a "navigate to new page/screen" affordance; single glyph, no `-Outline` sibling → registry `outline:null`). Appended to the **`preferredValues` of every icon instance-swap slot** — 50 slots across 28 component sets (Atoms 18 + Molecules 32; Organisms/Templates/Pages/Mockups have no icon instance-swaps) via `editComponentProperty`, idempotently. Code side: added the entry to `inventory.json` + `pnpm registry` (now 256 semantic names, 0 unresolved) + rebuilt icons dist → `<Icon name="Open" />` works. **⚠ Figma Library dirtied by the preferredValues writes → user must publish.**
- Phase 2 atoms — **form controls** (Toggle, Checkbox, RadioButton — all parity-checked at 96/100; **Toggle glyph sizing fixed 2026-06-04** — the pill SVG used a square `0 -960 960 960` viewBox at `36×20`, which uniformly downscaled the visible pill to `18.33×10`; now uses Figma's exact `0 0 36 20` export so the pill fills `36×20`, fixing both the standalone Toggle and Selection Control's Switch), **LETA Logo** (4 variants, 96/100 parity), **Elevation** (5-shadow showcase at `Atoms/Elevation`), **Buttons** (responsive Desktop + Mobile at `packages/components/src/Button/Button.tsx` + 11 Storybook stories under `Atoms/Button`; all 7 desktop variants × 8 mobile variants × 5 sizes (incl. Extra Small for Secondary/Icon Only) × all 6 Figma Type variants — No Icon / Leading Icon / Trailing Icon / Split Button / Icon Only / Prominent Icon Only; per-(Size, Type) padding + icon-size lookup matches Figma exactly, including asymmetric Leading/Trailing padding; border painted via `box-shadow: inset` so it doesn't shrink content area; FAB applies `--shadow-neutral-3`; default Storybook icons match Figma: `Proceed` for Leading/Trailing/Split, `Cancel` for Icon Only / Prominent Icon Only; parity 32/65 against `28:38245` Desktop / `2887:26272` Mobile), **Collapsed Sidebar Logo** (`packages/components/src/CollapsedSidebarLogo/CollapsedSidebarLogo.tsx` + 4 Storybook stories under `Atoms/CollapsedSidebarLogo`; cross-fades a 24×24 LetaLogo Symbol → 40×40 Ghost-button visual with the 20×20 `Sidebar` icon on `:hover, :focus-visible`, 300ms ease-out; outer always-`<button>`, no `disabled`, required `aria-label`; uses `box-shadow inset` to mimic Figma's `strokeAlign: INSIDE` so border doesn't shrink content area; parity 80/100 against `9132:34635`), **Avatar** (`packages/components/src/Avatar/Avatar.tsx` + 10 Storybook stories under `Atoms/Avatar`; 4 sizes × 2 render modes × 3 tones — `<img>` photo with onError fallback to monogram initials on `--surface-{notice,warning,neutral}-avatar-bg`; required `name` prop sources both the accessible name and auto-derived initials; `decorative` prop hides from SR when nested in an already-labeled interactive parent; non-interactive `<span role="img">`; parity 37/100 against `7446:22517` — passes CLAUDE.md gate, all 4 majors are documented paradigm artifacts), **Desktop Badge** (`packages/components/src/Badge/Badge.tsx` + 13 Storybook stories under `Atoms/Badge`; 10 semantic color variants × 3 icon positions (No Icon / Leading Icon / Trailing Icon) = 30 Figma variants; purely presentational `<span>`, no interactive states; `leadingIcon`/`trailingIcon` accept `IconName` from `@leta/icons` at 12×12; border via `box-shadow: inset` per project convention; color prop uses semantic names mapping to `--surface/border/text/icons-{color}-desktop-badge` token families; parity 62/100 against `68:36623` — passes CLAUDE.md gate, both visual majors are token-resolution artifacts of the parity checker), **Delivery Badges** (documentation-only story `Atoms/Badge/Delivery Badges`; 19 presets using `<Badge />`; labels/icons sourced directly from Figma node `68:36703`), **Tag** (`packages/components/src/Tag/Tag.tsx` + 4 Storybook stories under `Atoms/Tag`; single information/Sky Blue color × 3 visual states × optional trailing remove button; outer `<span>`, inner `<button>` for remove; hover/pressed via React state; parity checked), **Desktop Chip** (`packages/components/src/Chip/Chip.tsx` + 8 Storybook stories under `Atoms/Chip`; 4 types (No Icon / Leading / Trailing / Both) × 5 states (Idle / Hover / Pressed / Active / Focus) = 20 Figma variants; `<button>`, caller-controlled `active` prop; text-label-s-semibold; CSS injection for `:focus-visible`; **asymmetric per-type padding 2026-05-25**: No Icon and Leading+Trailing use 10/10 horizontal; Leading uses pl-10/pr-12 (icon-side tighter); Trailing uses pl-12/pr-10. Verified against Figma `7139:53343`; parity 62/100 — passes gate), **Flags** (`packages/components/src/Flag/Flag.tsx` + 3 Storybook stories under `Atoms/Flag` — Default, Sizes, Catalog; 265 country/territory flags backed by `country-flag-icons/react/3x2`; default 20×15 px; `YT-UNF` → `YT` and `GB-UKM` → `GB` normalization; grey-rectangle fallback in component for unknown codes (never exercised in stories — Catalog derives from `Object.keys(FlagIcons)` so only supported codes render); parity check N/A — no design tokens, all rendering from library SVGs; **stories cleaned up 2026-05-25**: removed invented `DeliveryRegions` and `UKNations` exports that had no Figma counterpart), **Featured Icon** (`packages/components/src/FeaturedIcon/FeaturedIcon.tsx` + 4 Storybook stories under `Atoms/FeaturedIcon`; 6 color variants × 2 sizes = 12 Figma variants; non-interactive `<span role="img">`; background + icon tint paired per color via `--surface-{color}-bg-subtle` / `--icons-{color}-default` tokens (Neutral uses `--surface-neutral-bg-tertiary`); container size from `--featured-icon-container-{size}` token (32px medium / 44px large); icon size 16px medium / 20px large; `--rounding-lg` corner radius; zero hardcoded values; visual parity confirmed via Dev MCP screenshot — all 12 variants match Figma; `figma_check_design_parity` REST endpoint blocked by figma-console-mcp 403 at time of implementation), **View Switcher Tab** (`packages/components/src/ViewSwitcherTab/ViewSwitcherTab.tsx` + 6 Storybook stories under `Atoms/ViewSwitcherTab`; 3 states (Default/Hover/Active); `<button>` with caller-controlled `active` prop; height 32px, `--padding-12px`, `--spacing-8px` gap, `--rounding-md` radius; bg idle=`--surface-neutral-segment-idle` / hover=`--surface-neutral-segment-hover` / active=`--surface-neutral-segment-selected`; text `text-label-m-semibold` idle=`--text-default-label-idle` / active=`--text-default-label`; active shadow `--shadow-neutral-2`; optional `leadingIcon` at 16px; focus-visible CSS injection), **Segment Switcher Tab** (`packages/components/src/SegmentSwitcherTab/SegmentSwitcherTab.tsx` + 6 Storybook stories under `Atoms/SegmentSwitcherTab`; same pattern as ViewSwitcherTab but larger: height 40px, `text-label-l-semibold`, **icon 20px constant across Active/Default/Hover — flattened 2026-05-28 after the designer collapsed the prior 20/24 split and bound the size to a Figma variable (`Icons/medium`, `VariableID:8403:24088`)**; same `--surface-neutral-segment-*` token family), **Page Tab** (`packages/components/src/PageTab/PageTab.tsx` + 4 Storybook stories under `Atoms/PageTab`; 4 states (Idle/Hover/Pressed/Active); `<button>` flex-col, height 40px, gap 4px; text `text-label-m-semibold` idle=`--text-default-label-idle` / hover=`--text-default-label` / active+pressed=`--text-secondary-label`; active 2px bottom indicator bar `--icons-secondary-default`; no bg/border/shadow), **Wizard Tab** (`packages/components/src/WizardTab/WizardTab.tsx` + 6 Storybook stories under `Atoms/WizardTab`; 5 states (Active/Hover/Idle/Pressed/Inactive); extends PageTab with step circle: 20×20 `--rounding-round`, filled=`--surface-secondary-bg`+`--text-on-color-label`, idle=`--surface-neutral-bg-tertiary`+`--text-default-label-idle`; step text `text-body-s-medium`; inactive `opacity: 0.6` (hardcoded — `--opacity-60` token pipeline bug resolves to `60px`); active 2px bar same as PageTab), **Title** (`packages/components/src/Title/Title.tsx` + 2 Storybook stories under `Atoms/Title`; non-interactive heading from Figma `6767:26920`; 2 variants — `page-dialog` (Heading/M/SemiBold, 24/28/-0.4) and `card` (Body/L/SemiBold, 16/24/-0.28); `text` prop drives content (default "Heading 1"); `as` prop controls semantic HTML level independent of visual size; color always `--text-default-heading`), **Map Icon** (`packages/components/src/MapIcon/MapIcon.tsx` + 7 Storybook stories under `Atoms/MapIcon`; **renamed from "Map Icon Type" 2026-05-25 to match Figma**; **5 variants 2026-05-25**: Object Pin / Numeric Pin (node `10046:2330`) / Badge (renamed from Object Badge) / Event Pin / Bike Delivery (NEW node `8658:14591` — 19×32 raster illustration of a top-down bike courier, inlined as base64 PNG in `bike-delivery-asset.ts`); interactive `<div>` supporting onClick/onMouseEnter for tooltips/drawers/modals; `variant` prop selects shape; `color` prop sets the *inner* fill (default `--surface-secondary-bg`); **layered structure for white-background visibility**: outer shape always white (`--surface-neutral-bg-primary`) with 0.2px light-gray stroke (`--border-neutral-default`), inner ellipse holds the configurable colour. Object Pin / Numeric Pin: 26×32 teardrop from Figma path; Badge: 32×32 circle with 14.5r inner; Event Pin: 32×32 — white teardrop background + dark `Location-Check` Material Symbol overlay (where-to-vote-rounded). **Shape-aware shadows via `filter: drop-shadow(...)`** stacked 3-layer — replaces previous rectangular `box-shadow` frame. Numeric Pin renders text inside the pin; Badge renders text or icon. Text uses `text-label-m-semibold` (Label/M/SemiBold — 14px / 20px / -0.28) — Figma binding fixed 2026-05-25. Visual parity confirmed via Dev MCP + 5× Storybook zoom; `figma_check_design_parity` REST 403 still blocking), **Progress Tracker** (`packages/components/src/ProgressTracker/ProgressTracker.tsx` + 2 Storybook stories under `Atoms/ProgressTracker` — Default, Catalog; 14×14 donut ring SVG with `role="progressbar"`; gray track `--surface-neutral-progress-bar-track` + green arc `--surface-success-task-progress-bar`; 4 Figma variants (10/50/75/100%) from `5608:216176`, generalised to any `value: 0-100`; uses `<circle>` with `strokeDasharray` and `transform="rotate(-90)"` so the arc starts at 12 o'clock and sweeps clockwise; ARIA `valuemin/valuemax/valuenow`; also used as the leading indicator inside MobileBadge's Progress variant), **Carousel Pagination** (`packages/components/src/CarouselPagination/CarouselPagination.tsx` + 2 Storybook stories under `Atoms/CarouselPagination` — Default, Catalog; non-interactive `<div role="group">`; horizontal row of indicators showing current page in a carousel; 3 Figma variants (Page 1 / Page 2 / Page 3) from `8667:15335`, generalised to support any `total`; active indicator = 20×6 pill `--surface-primary-active-carousel-pagination` (LETA red) with `--rounding-round`; inactive = 6×6 circle `--surface-neutral-inactive-carousel-pagination` (medium gray); 8px gap; 1-indexed `current` prop; smooth width transition for animated page changes), **Mobile Nav Tab** (`packages/components/src/MobileNavTab/MobileNavTab.tsx` + 3 Storybook stories under `Atoms/MobileNavTab` — Idle, Active, Catalog; vertical bottom-nav cell `<button>`; 80×58, content bottom-aligned, 4px gap; 24px icon over 14px Label/M/Regular text; 2 Figma variants from `8417:23519`; Idle = `--icons-neutral-idle` + `--text-default-label-idle` (muted gray); Active = `--icons-primary-default` + `--text-primary-label` (LETA red); caller-controlled `active`; mobile-only, no hover; focus via `:focus-visible`), **Mobile Segment** (`packages/components/src/MobileSegment/MobileSegment.tsx` + 3 Storybook stories under `Atoms/MobileSegment` — Default, Active, Catalog; pill-shaped `<button>`; 2 Figma variants (Default/Active) from `3038:38166`; Default = `--surface-neutral-segment-idle` + `--text-default-label-idle`; Active = `--surface-neutral-segment-selected` + `--text-default-label` + `--shadow-neutral-2`; Label/M/SemiBold typography; `--padding-12px` horizontal, `--padding-6px` vertical; `--rounding-round`; caller-controlled `active`; focus via `:focus-visible` CSS injection), **Mobile Featured Icon** (`packages/components/src/MobileFeaturedIcon/MobileFeaturedIcon.tsx` + 4 Storybook stories under `Atoms/MobileFeaturedIcon` — Default, Basic, Filled, Catalog; non-interactive `<span role="img">`; 6 colour variants × 2 variants (Basic/Filled) × 3 sizes (S=20px / M=32px / L=56px) = 36 Figma variants from `4136:74148`; Filled = circular tinted background (`--surface-{color}-bg-subtle`) + centred smaller icon; Basic = icon only, no background; icon colour `--icons-{color}-default`; container sizes 20/32/56px; Filled-variant icon sizes 16/20/32px; Basic-variant icon sizes 20/24/56px (Medium has 4px padding around the icon per Figma)), **Mobile Chip** (`packages/components/src/MobileChip/MobileChip.tsx` + 5 Storybook stories under `Atoms/MobileChip` — NoIcon, WithLeadingIcon, WithTrailingIcon, WithLeadingAndTrailingIcons, Catalog; touch-first `<button>` — **no hover state**; 4 types × 4 states (Idle / Pressed / Active / Focus) = 16 Figma variants from `8269:29237`; uses mobile-chip token family (`--surface-neutral-mobile-chip-{idle,pressed,active}`, `--border-neutral-mobile-chip-{idle,pressed}`, Active border uses shared `--border-secondary-chip-active`); `text-body-s-semibold` text; 28px height; 1px border via `box-shadow: inset`; `--rounding-round`; asymmetric padding pl-10/pr-12 (Leading) or pl-12/pr-10 (Trailing) or 10/10 (Both) or 12/12 (No Icon); 16px icons; Pressed tracked via touch + mouse-down/up + mouse-leave for desktop-browser preview; Active caller-controlled; Focus via `:focus-visible` CSS injection), **Mobile Badge** (`packages/components/src/MobileBadge/MobileBadge.tsx` + 4 Storybook stories under `Atoms/MobileBadge` — Default, WithLeadingIcon, WithTrailingIcon, Catalog; non-interactive `<span>`; 8 colour variants (blue/green/red/orange/yellow/purple/grey/coral-red) × 3 icon-position types (No Icon / Leading / Trailing) = 24 Figma variants from `3100:17588`; uses mobile-badge token family (`--surface/{semantic}/mobile-badge`, `--border/{semantic}/mobile-badge`, `--text/{semantic}/label`); `text-caption-l-semibold` (10/16/-0.28); 14px icons coloured to match text; asymmetric padding pl-6/pr-8 (Leading) or pl-8/pr-6 (Trailing) or 8/8 (No Icon); 1.5px border via `box-shadow: inset`; `--rounding-xl` (12px) corners; 20px height. Progress Badge variant added 2026-05-25 — `variant="progress"` + `progressValue` props compose with `<ProgressTracker>` on the leading side, always renders in grey/neutral palette per Figma), **Delivery Map Icons** (documentation-only story `Atoms/MapIcon/Delivery Map Icons`; 15 presets using `<MapIconType />`; 6 sections: Drivers (3), Users (3), Orders (3), Shops (2), Many Orders cluster (3 — `variant="badge"`), Events (1 — `variant="event-pin"`); colours/icons sourced directly from Figma node `3129:23003`; includes `AllStatuses` and `GridOverview` stories), **Map Zoom Control** (`packages/components/src/MapZoomControl/MapZoomControl.tsx` + 4 Storybook stories under `Atoms/MapZoomControl` — Default, ZoomInDisabled, ZoomOutDisabled, Catalog; fixed-layout 40×96px floating overlay control; outer `<div>` container with `--surface-neutral-bg-primary` bg, `--stroke-sm` inset border, `--rounding-lg`, `--padding-8px`, `--spacing-16px` gap, `--shadow-neutral-3`; two `<button>` children (`Add` / `Minus` icons at 24px, `--icons-neutral-default`) separated by a zero-height `<div>` divider with `border-top: var(--stroke-sm) solid var(--border-neutral-default)`; `onZoomIn` / `onZoomOut` callbacks; `disableZoomIn` / `disableZoomOut` props apply `opacity: 0.4` + `cursor: not-allowed`; LETA focus ring via `ensureStyles()`; parity 64/100 against `7900:163823` — gate Pass with note, 2 majors are token-resolution artifacts, 4 componentAPI minors are paradigm differences).

- **Molecules Batch 1 — Filter & Tab Controls** (shipped 2026-05-28): **Top Filter** (`packages/components/src/TopFilter/TopFilter.tsx` + 4 stories under `Molecules/TopFilter`; status-filter pill button for table toolbars; 2 types Basic/Advanced × 6 states (Idle/Hover/Pressed × ±Selected) = 12 Figma variants from `1572:12741`; `<button>` with internal hover/press + caller-controlled `selected` + `aria-pressed`; Advanced renders trailing 16px Chevron-Down — the dropdown overlay it would open is a separately-deferred molecule, so `onClick` is the open signal only; uses dedicated `--surface-neutral-top-filter-tab-{idle,hover,pressed,active}` + `--border-neutral-top-filter-tab-{idle,hover,pressed}` + `--border-secondary-top-filter-tab-active` token families; label `text-label-m-semibold` / `--text-default-label`; height 40, `--rounding-lg`, gap `--spacing-8px`, asymmetric pr 10/12 by type; visual parity confirmed via `figma_capture_screenshot 1572:12741`, `figma_check_design_parity` REST 403 still blocking), **Top Filter Section** (`packages/components/src/TopFilterSection/TopFilterSection.tsx` + 3 stories under `Molecules/TopFilterSection`; the filter-bar row from `9469:84304`; data-driven `filters[]` + `onFilterClick(index)`; non-interactive `<div>` flex row, `--spacing-12px` gap, height 40; filters are independent toggles — no selection model enforced), **Desktop Segment Control** (`packages/components/src/DesktopSegmentControl/DesktopSegmentControl.tsx` + 4 stories under `Molecules/DesktopSegmentControl` — ViewSwitcher / SegmentSwitcher / WithLeadingIcons / Catalog, **all label-only by default to match Figma's `Show Leading Icon: false`**; segmented-control container from `5778:230039`; 2 variants — `view` wraps `ViewSwitcherTab` with track pad `--padding-4px` (height 40 derived), `segment` wraps `SegmentSwitcherTab` with track pad `--padding-8px` (height 56 derived); track bg `--surface-neutral-segment-track`, gap `--spacing-8px`, `--rounding-lg`; data-driven `value`/`onChange` enforce single-active; `role="group"` + per-tab `aria-pressed`), **Page Tabs Control** (`packages/components/src/PageTabsControl/PageTabsControl.tsx` + 3 stories under `Molecules/PageTabsControl`; full-width tab bar with demarcator from `3907:71981`; 2 variants — `basic` wraps `PageTab`, `wizard` wraps `WizardTab` with `step` defaulting to 1-based index and an `inactive` flag for future steps; demarcator = container `border-bottom: var(--stroke-sm) solid var(--border-neutral-default)` so the active tab's 2px indicator sits on the baseline; `role="tablist"` + per-tab `role="tab"` + `aria-selected`; **Scrollable Figma variant deleted by designer 2026-05-27 and not implemented**).

- **Molecules Batch 2 — Phase 4 (in progress, 1/10 built)** — descriptions for all 10 Phase-4 components written to Figma `description` fields (2026-06-04, one `figma_execute` batch) + the "Mulitple Countries"→"Multiple Countries" variant-value rename on Leading Input Field Element (`7111:43945`); **library not yet published**. Built so far: **Selection Control** (`packages/components/src/SelectionControl/SelectionControl.tsx` + 7 stories under `Molecules/SelectionControl` — Checkbox / RadioButton / Switch / Indeterminate / Disabled / WithTrailingIcon / Catalog; labelled selection-input wrapper composing the Checkbox / RadioButton / Toggle atoms; 3 `variant`s × state = 10 Figma variants from `37:362` (Checkbox: Idle/Active/Indeterminate/Disabled; Radio + Switch: Idle/Active/Disabled — no Indeterminate); **layout is variant-dependent: Checkbox/Radio lead with the control (label trailing), Switch leads with the label (toggle trailing)** — verified from live Figma, not inferred; `forwardRef<HTMLDivElement>`, inline-flex row, gap `--spacing-8px`, `align-items:center`; label `text-label-m-regular` / `--text-default-label`; optional `trailingIcon?: IconName` renders an 18px outlined help icon after the label (Figma `Show Label Trailing Icon`, default Info); **a11y: atoms spread `id` onto their `<input>`/`<button>`, so a sibling `<label htmlFor>` toggles natively + `aria-labelledby` names it** — no nested-`<label>` problem, no extra focus ring (atoms own theirs); `indeterminate` checkbox-only; **Disabled dims the row with literal `opacity: 0.6`** because `--opacity-60` resolves to the buggy `60px` (cf. WizardTab); rows measure 73×30 (cb/radio) and 87×40 (switch) — byte-for-byte vs Figma; `label` is intentionally **required** (the Figma `Show Label Container=false` bare-glyph case maps to using the atoms directly, per the approved spec); typecheck clean, label-click toggle + a11y roles/names verified in Storybook; parity `figma_check_design_parity 37:362` = 55/100, 0 critical, 0 major/minor in visual/spacing/typography (the 1 major is `accessibility/semanticElement` paradigm noise; minors/infos are componentAPI props-vs-variant paradigm) — **gate Pass**). Repo-wide lint is environment-broken (no `eslint.config.*` exists anywhere); typecheck is the gate.

- **Phase 4 Molecules Batch 2 COMPLETE (10/10)** — Selection Control, Shortcut, Desktop Progress Indicator, Stepper, Leading Input Field Element, Trailing Input Field Element, Pagination, Breadcrumbs, Footer Frame, Modal Headers. All parity-gated.

In progress:
- **Molecules Batch 3** — done so far: **Notification Banners `3811:65015`** (2026-06-09; 5 types × 2 variants, parity 58/100 gate Pass; FooterFrame stories updated to use real component), **Desktop Metric Cards `4239:74634`** (2026-06-10; interactive `<button>` KPI card wrapping `ContentPrimitives type="metrics"`; 16 variants = 4 states (Default/Hover/Pressed/**Focus**) × 4 variance; **Default border `--border-neutral-default` 1px, Hover adds `--shadow-neutral-1` drop shadow, Pressed darker `--border-neutral-card-pressed` 1.5px, Focus = standard `:focus-visible` ring (1.5px `--border-secondary-component-focus`, 4px offset)**; native `<button>` border reset with `border:none` (UA 2px outset was overriding the inset box-shadow border); variance badge map Neutral→neutral / Positive→success+Up-Arrow / Negative→error+Down-Arrow via new ContentPrimitives `metricVarianceColor`/`metricVarianceIcon` props; `state` prop forces a visual state for the Catalog; parity 54/100 gate Pass), **Option Cards `9894:18459`** (2026-06-10; designer simplified to single `State` axis Idle/Hover/Pressed/Active/**Focus** — no more "Choice Card"; `<label>` + `ContentPrimitives type="utility"` + `RadioButton`; Active border `--border-secondary-card-active`; **Focus = card-level ring via `.leta-option-card:has(:focus-visible)`** (nested radio is the focus target) + `state` override; parity 50/100 gate Pass), **Configuration Card `9617:18100`** (2026-06-10; ships `ConfigurationCardRow` sub-component; toggle-able section — header `utility` CP + `Toggle`, body `children` rows + footer reusing `FooterFrame variant="card"` with subtle `NotificationBanner` + Secondary/Primary buttons; collapses to header-only when disabled; parity 43/100 gate Pass). **ContentPrimitives fix (2026-06-10):** eyebrow/list-row help markers defaulted to icon `Help` (registry `outline: null` → silently rendered filled); switched to `Question` (has real outline = Figma `Icon/Question-Outline`) + added colors (`--icons-neutral-idle` eyebrow, `--icons-neutral-default` list row) + centered Metrics trailing content vertically (`TrailingContent` `alignSelf: stretch`). Next candidates: Confirmation Dialog, Tooltip, Accordion, Empty State, etc.

- **Molecules Batch 4 (in progress)** — **Desktop Toast `68:36772`** built (2026-06-10; `packages/components/src/Toast/` + 5 stories `Molecules/Toast` — Success/Warning/Error/WithCTA/Catalog; presentational `role="status"` surface, 512px, white bg + `--border-neutral-default` inset + `--shadow-neutral-2`, `--rounding-xl`, padding 16; 24px status icon Success `Check-Circle`/`--icons-success-default` · Warning `Warning`/`--icons-warning-default` · Error `Cancel-Circle`/`--icons-error-default`; title `body-l-medium`/`--text-default-heading` + subtitle `body-m-regular`/`--text-default-sub-body`; dismiss × (**Ghost / Prominent Icon Only / Medium, 40×40** — per Figma `6352:27457`; gives the proper hover fill `--surface-neutral-ghost-button-hover`, not Plain) + optional CTA slot; `duration` auto-fires `onDismiss` (~7s, bottom-right placement owned by consumer); parity 29/100 gate Pass — 0 critical, all majors documented artifacts: bg/border token-resolution, gap 113 = fixed-itemSpacing-vs-`space-between` idiom, enum naming, COMPONENT_SET chrome). **OptionCard radio fix (2026-06-10):** Figma's Option Card Content frame is `crossAxis: MIN` (radio top-aligned with the title), but the standalone Utility primitive is `CENTER` — added an opt-in `contentAlign?: 'center'|'top'` prop to `ContentPrimitives` (threaded through `HeadingLayout` + `TrailingContent` `verticalAlign`); OptionCard passes `"top"`. Metric card trailing (MetricsLayout) keeps the default centered/stretch. Also rebuilt the stale `@leta/components` dist (was Jun-9). Descriptions written to Figma for Toast `68:36772`, Empty State `7339:29150`, Tooltip `1178:15588`, Input Fields `38:42` (notes planned split), Desktop Menu Options `1531:5056` — **pending user publish**.

**Tooltip `1178:15588`** built (2026-06-10; `packages/components/src/Tooltip/` + 9 stories `Molecules/Tooltip` — Small/Standard/Coachmark/Flyout/Advanced + MobileSmall/MobileStandard/MobileCoachmark + Catalog; presentational `role="tooltip"` surface — triggering/positioning owned by consuming prototypes; white `--surface-neutral-tooltip` + `--border-neutral-default` inset + `--shadow-neutral-2`, radius **`--rounding-lg` (8) for Small/Standard, `--rounding-xl` (12) for Coachmark/Flyout/Advanced (both platforms)** — confirmed per-variant via bridge (`Rounding/lg` vs `Rounding/xl`); **configurable caret** via `caretSide` (top/bottom/left/right, default bottom) + `caretAlign` (start/center/end, default center) — a 16px rotated bordered square straddling the edge (no caret-position matrix in Storybook, per designer: prototypes position it by trigger); 5 variants — Small (label), Standard (paragraph), Coachmark (title+body+`step`/`totalSteps` "1 of N"+Skip/Next), Flyout (title+divider+body+Cancel/Delete-destructive), Advanced (Header Title + bottom divider, then Body gap-20 with the `details` ReactNode slot [2-col metadata grid + a "Configuration Control" = divider·switch·divider] + the `cta` ReactNode slot [full-width Dispatch] — the toggle lives **inside** the details slot per Figma, not component chrome); reuses Button; `platform="mobile"` ⇒ small/standard/coachmark only; parity 23/100 gate Pass — 0 critical, majors are bg/border token-resolution + enum-naming, the 26 info are Figma's 25-property mega-set vs the clean React API).

**Empty State `7339:29150`** built (2026-06-10; `packages/components/src/EmptyState/` + 5 stories `Molecules/EmptyState` — Default/Small/NoIcon/WithCTA/Catalog; centred placeholder column (width 300, gap 20, centered): illustration + heading + copy + optional CTA (`children`); **14 `type` presets** (a `PRESET` map of icon+outlined+heading+copy: no-results Search · no-reviews Reviews · **no-broadcast-logs `Broadcast` (renamed from "Broadcast starting soon"/Clock 2026-06-11)** · image-upload Image-Upload · file-upload Document · no-suspensions Deactivate · no-items Orders · no-orders Open-Box · no-network Poor-Network · new-update Fix · no-data Data-Table · no-trips Trips-Routing · no-drivers Motorcycle · no-products Inventory) × `size` (small=64px icon/`body-l-semibold` heading, large=100px/`heading-m-semibold`); illustration = icon (`--icons-neutral-default`) over an absolutely-positioned ellipse ground-shadow (`--surface-neutral-bg-tertiary`, 160×38 large / 100×24 small); heading `--text-default-heading` centered + subtext `body-m-regular`/`--text-default-sub-body`; `showIcon=false` ⇒ Figma's "No Results (No Icon)"; `heading`/`description` overrides; Container SLOT → `children` CTA. Parity 52/100 gate Pass — 0 critical; majors = transparent bg (intentional — content block sits on consumer surface; Figma frame fill is artboard backdrop), enum-naming, COMPONENT_SET chrome; info = per-type Figma TEXT props vs the PRESET map.)

**Input Field (core) `38:42`** built (2026-06-10; `packages/components/src/InputField/` + 8 stories `Molecules/InputField` — Basic / Leading / Trailing / Selection / Stepper (per-variant) + States + WithCounter + Catalog; the **split-out Input Field type** only (Search Bar / Text Area / File Upload remain deferred siblings); `variant` = basic / leading / trailing / selection / stepper; **states are runtime** (a real `<input>`; focus via React focus-state → border `--border-secondary-component-focus`; `error` → `--border-error-default` + `Icon/Error` + `--text-error-label` msg; `warning` → `Icon/Warning` + `--text-warning-label` msg, border unchanged; `disabled` → `--surface-disabled-input-field`/`--border-disabled-default`; filled vs placeholder text colour natural); field box h40, `--rounding-lg`, pad 12h/10v, `--surface-neutral-input-field`, inset `--stroke-xs` border; label section (Label/M/Medium `--text-default-label-idle` + optional Info icon + Optional/Required tag + optional `labelToggle` slot); helper/message section (Label/S/Regular `--text-default-helper`) with optional right-aligned counter; **ReactNode slots** `leadingElement`/`trailingElement`/`stepper` compose the existing `LeadingInputFieldElement`/`TrailingInputFieldElement`/`Stepper` molecules; selection variant = trailing Chevron-Down + `onSelectionClick`. **Two post-build fixes (2026-06-11):** (1) input rendered serif — removed `font: inherit` from the input reset so the `text-label-m-regular` class drives the font; (2) Basic/Selection field boxes collapsed to 20px — `flex: 1` on a direct child of the vertical root sets flex-basis 0% and overrides `height: 40`; now only the Leading/Trailing field box (inside the horizontal Container) uses `flex: 1`, others use `width: 100%`. Parity 9/100 gate Pass — 0 critical, 0 visual/spacing/typography majors; the 2 majors (variant-values, size) are the intentional split + COMPONENT_SET chrome; the low aggregate is the clean API vs the 56-variant mega-set's 27 properties (informational).)

**Input Fields siblings — Text Area / Search Bar / File Upload (desktop) `38:42`** built (2026-06-11; the other three types split from the mega-set):
- **Text Area** (`packages/components/src/TextArea/` + 5 stories — Basic/Filled/Error/Disabled/Catalog): multi-line `<textarea>`, field box min-h 120, `--rounding-lg`, pad 12h/10v, `--surface-neutral-input-field`, inset border; **in-field counter** (Label/S/Regular, bottom-right); label + helper/warning+icon/error+icon sections shared with InputField; runtime states (focus→component-focus, error→error-default, disabled). **Rich (formatting-toolbar) variant deferred.** Parity 49/100 gate Pass (0 critical, 0 major; all paradigm vs the 38:42 set).
- **Search Bar** (`packages/components/src/SearchBar/` + 4 stories — Default/Filled/Disabled/Catalog): compact field h40, **`--surface-neutral-search-field`**, leading `Search` icon (16px), `text-label-m-regular` input; focus→component-focus; optional `onClear` shows a trailing × when filled; optional label/helper. **Mobile / Web Map / Mobile Map (results-dropdown) variants deferred.**
- **File Upload** (`packages/components/src/FileUpload/` + 3 stories — Default/Disabled/NoLabel): label (default tag Optional) + **dashed-border drop zone** (real CSS `dashed`, min-h 180, `--surface-neutral-input-field`) with prompt + Secondary "Browse to upload" Button (triggers a hidden `<input type=file>`) + max-size hint; drag-drop via `onFiles`; `children` slot for an uploaded-file-cards list. **Mobile + Uploading/Uploaded states (File Upload Cards list) deferred.**
All three visually verified vs Figma renders; same paradigm-only parity pattern as Input Field (0 critical, 0 visual majors). 

**Data Entry restructure + fixes (2026-06-11) — supersedes the Input Field/siblings notes above.** The Figma mega-set was renamed **"Data Entry" `38:42`** with `Type` = Search Input / Input Field / Text Area / File Upload / **Select** / **Stepper Input**, and all stories moved into a `Molecules/Form Controls/` Storybook folder (order enforced in `apps/storybook/.storybook/preview.tsx` storySort). Code changes:
  - **InputField** variants reduced to `basic | leading | trailing` (the old `selection`/`stepper` variants removed). Shared label/field/message scaffolding extracted to internal `InputField/FieldChrome.tsx`, reused by Select + StepperInput.
  - **Select** (`packages/components/src/Select/`) — new standalone component (was InputField `selection`). Read-only field box + `Chevron-Down`, `onSelectClick`. **Token fixes 2026-06-11:** field fill `--surface-neutral-selector-field` (not input-field), chevron `--icons-neutral-default` (not idle). Stories Default/Filled/**Warning**/Error/Disabled/Catalog (Warning = warning icon + msg, border unchanged — the only Data Entry types with a Warning state are Input Field + Select).
  - **StepperInput** (`packages/components/src/StepperInput/`) — new standalone component (was InputField `stepper`); composes `<Stepper>`.
  - **Search Input → 4 separate components** (renamed from SearchBar; the desktop/mobile split mirrors FileUpload/MobileFileUpload). A shared internal `SearchInput/SearchField.tsx` renders the 40px field row (leading Search icon, `type="text"` input + hidden `::-webkit-search-cancel-button`, optional clear × in `--icons-neutral-default`), parameterized by surface+radius; the four components compose it: **`SearchInput`** (desktop, `--surface-neutral-search-field`/`--rounding-lg`), **`MobileSearchInput`** (pill, `--surface-neutral-input-field`/`--rounding-round`), **`MapSearchInput`** (Web Map; 480px), **`MobileMapSearchInput`** (Mobile Map; pill, field + gap + separate `--rounding-xl` panel). The map results lists are the not-yet-built Desktop Menu Options `1531:5056` — `Placeholder` stands in.
  - **MapSearchInput rebuilt with all 5 Figma states (2026-06-11 pm)** via a `state` prop — `idle` (field only) / `no-history` ("No recent searches" helper) / `active-history` ("Recent Searches" heading + Clear All Plain button + list) / `empty-results` (Top Section filter `Chip`s + "Results (0)" heading + reuse of the **`EmptyState`** molecule `type="no-results" size="small"`) / `active-results` (filter chips + "Results (N)" + list). Open states render as ONE bordered card (`--rounding-xl` + real CSS `border` + `overflow:hidden`) with the alt-search-field field on top (`SearchField noBorder`, bottom divider) — avoids the double-inset-border seam. Reuses `Chip` (filters) + `EmptyState` + `Button` Plain + `Placeholder` (the menu list, default). Stories: Idle/NoHistory/ActiveHistory/EmptyResults/ActiveResults/Catalog.
  - **Polish fixes (2026-06-12):** (1) Map headers ("Recent Searches"/"Results (N)") use **`text-heading-s-semibold`** (20px) — were heading-m (24). (2) Both map components carry a **drop shadow** — `MapSearchInput` `--shadow-neutral-2` (on the field wrapper when idle, the card when open); `MobileMapSearchInput` `--shadow-neutral-1` (on the pill field + the results panel). (3) **EmptyState `size` renamed `small|large` → `mobile|desktop`** to mirror the Figma `Size` axis (Mobile = 64px icon/`body-l-semibold`, Desktop = 100px/`heading-s-semibold` — **updated from `heading-m` 2026-06-13** to match the designer's resized empty-state heading); stories now show both via `Desktop`/`Mobile` catalogs and the bogus `WithCTA` story was removed (CTA is per-type, the Container slot). `MapSearchInput` empty-results passes `size="mobile"`. (3b) **Map panel heights pinned to Figma** — Empty/Active Results are a **fixed 400px** dropdown region (whole card 440; via `RESULTS_PANEL_HEIGHT`), body `flex:1` so the no-results `EmptyState` **centres** in the ~292px below the Top Section (was hugging → wrong height + top-aligned); No History 90 / Active History 288 / Mobile Map Active 296 also match (Placeholder min-heights tuned). Parity `7339:29044` = 76/100, 0 critical, 0 real visual/spacing majors (the 1 gap "major" is the hidden-label root-gap; visible field↔panel gap is 0 both sides) — gate Pass. (4) **Badge `primary` (Figma "Coral Red") text fixed** `--text-primary-label` (bright red) → **`--text-default-label`** (dark) — Figma uses the default label colour; this propagates to every `<Badge color="primary">` consumer (Delivery badges, TopFilter/TopFilterSection, Modal Headers). MobileBadge `coral-red` was already correct.
  - **FileUpload** — **removed the `disabled` state** (not in Figma). Added desktop **Uploading/Uploaded** states via new **`FileUploadCard`** (`packages/components/src/FileUpload/FileUploadCard.tsx`; `--surface-neutral-bg-tertiary` card, Loading/`Check-Circle` glyph, filename `label-m-semibold`/`--text-default-heading` + size `label-s-regular`/`--text-default-sub-body`, Ghost Icon-Only remove ×, reuses `DesktopProgressIndicator variant="upload"` while uploading). Stories: Default/Uploading/Uploaded/NoLabel.
  - **MobileFileUpload** (`packages/components/src/MobileFileUpload/`) — new component for the Figma File Upload Mobile variant: 3-up grid of 111×104 tiles. "Add Photo" placeholder = **dashed** `--border-neutral-default` border + **filled** `Image-Upload` 40px (`--icons-neutral-idle`) + "Add Photo" label (`text-label-s-regular`/`--text-default-label-idle`) on `--surface-neutral-input-field`; uploaded thumbnail + red `Delete` badge on a `--surface-neutral-bg-primary` circle. Stories EmptyState/**Uploaded** (realistic picsum sample photos)/Catalog. (Fixed 2026-06-11 pm: dashed border, filled icon, label-s-regular, real images, story renamed Filled→Uploaded.)
  - **`Button` ghost idle made transparent (global fix, 2026-06-11).** The `ghost`/mobile-`ghost` **idle** + **focus** token rows resolved to opaque **white** (`--surface/--border-neutral-ghost-button-idle` → `Colors/Neutral/white`), so ghost icon-only buttons showed a **white box on non-white surfaces** (the File Upload card ×, Text Area Rich toolbar). Figma's ghost idle has no fill/stroke. Fix: set ghost idle/focus to the bare `T(null, null, '--text-default-label')` pattern (hover/pressed unchanged). Verified no regression on Toast / Tooltip / MapZoomControl (white surfaces) — they already read as transparent.
  - **TextArea Rich** — added `variant: 'basic' | 'rich'`; rich = a continuous bordered card where the **Field** carries border top/right/left + top-rounded corners (Figma `strokeSides:[1,1,0,1]`, `corners:[8,8,0,0]`) and the **Footer toolbar** carries a full border + bottom-rounded corners (`--surface-neutral-bg-primary`, pad 8/12): Bold/Italic/Underline Ghost Icon-Only Buttons (left) + Attachment (Ghost) + Send `Up-Arrow` (Primary Icon-Only) (right). (Fixed 2026-06-11 pm: the first attempt used an `overflow:hidden` container with an inset box-shadow border that the child backgrounds covered → no visible border; switched to real per-side CSS borders per Figma.) Story `Rich` added.
  - **`Placeholder`** (`packages/components/src/Placeholder/`) — generic dashed stand-in box (`label?`); originally used inside the Map search panels (now replaced by real menu options — see below). Story at `Molecules/Placeholder`.

- **Menu Options + Dropdowns (re-architected 2026-06-12 pm against the updated Figma)** — the design system was reorganized into FOUR components: **Menu Options = individual rows**, **Drop downs = assembled overlay panels** that compose those rows. Descriptions were written to all four Figma component descriptions (set via bridge; pending publish).
  - **`DesktopMenuOptions`** (`1531:5056`, `packages/components/src/DesktopMenuOptions/`) — ONE component, `type` prop = **14 Types** (combobox · dropdown-basic · dropdown-advanced · dropdown-destructive · checkbox-selection · radio-selection · day-cell · time-picker · filter-group · sidebar-main · sidebar-main-icon · sidebar-sub · side-tab · pagination). **Mobile-day-cell was removed (now mobile-only).** Runtime hover/pressed + caller `active`/`selected`/`disabled` + a **`state` override prop** (idle/hover/pressed/active/disabled/*-selected) so catalogs show every Figma state. Per-type surface/border/text resolvers. Reuses `ContentPrimitives type="utility"` (dropdown-advanced: `text=`, `showVisualAnchor={false}`), `SelectionControl`, `Chip`, `Shortcut`/`Badge`/`Icon`.
  - **`MobileMenuOptions`** (`8338:35033`) — now a `type` prop = **4 Types** (combobox · mobile-day-cell · checkbox-selection · radio-selection); Idle/Pressed/Active/Disabled (no hover).
  - **`DesktopDropdowns`** (`8230:26475`, NEW) — ONE component, `variant` prop = **13 assembled panels** (combobox · combobox-search · combobox-search-empty · combobox-create · combobox-create-empty · actions · user-menu · sort · basic-filter · filter-group · filter-group-empty · timepicker · stacked-list). Shared card chrome (bg-primary, 1px border, `--rounding-xl`, **shadow-neutral-3**, clipped). Composes `DesktopMenuOptions` rows + `SearchInput`/`EmptyState`/`NotificationBanner`/`Avatar`/`Button`. `options`/`children` slots.
  - **`MobileDropdown`** (`8338:35339`, renamed from MobileCombobox) — panel stacking `MobileMenuOptions` rows.
  - **Storybook** ("like Buttons" two-file pattern): `Molecules/Menu Options/Desktop` (all 14 row types, every state) + `/Mobile` (4 types); `Molecules/Drop downs/Desktop` (`DesktopDropdowns`, 13 variants) + `/Mobile` (`MobileDropdown`). `MapSearchInput`/`MobileMapSearchInput` rewired (Placeholder removed). **EmptyState** split into `/Desktop` + `/Mobile` (one story per Figma type per size; `Default`/`NoIcon` removed).
  - **✅ Correction pass DONE (2026-06-13):** full variant-by-variant parity pass against Figma, verified via Storybook screenshots on port 6107. **DesktopMenuOptions:** sidebar nav icons now **always outlined** (was filled when active) with idle/hover/pressed=`--icons-neutral-idle`, active=`--icons-neutral-default`; **sidebar-main-icon centered** (`justifyContent:center`, was left); **combobox** Active = light-grey fill + trailing **`Check`** icon (no chevron); combobox/dropdown-basic leading icons render **dark** (`--icons-neutral-default`), **dropdown-destructive leading icon is red** (`--icons-error-default`); **dropdown-advanced** passes `showInteractiveElements={false}` + 20px chevron (kills the excess right-padding in Stacked List); **filter-group** replaced the freeform `tags` prop with a single derived **"{n} selected" `Chip`** (`selectedCount` + `onDeselectAll`, trailing `Cancel`). **MobileMenuOptions:** combobox Active = trailing `Check`. **DesktopDropdowns** rewritten + made **interactive (internal state)**: real **`Pagination` variant="combobox"`** footer (count + prev/next **centered** as one group, gap 12 — was a wrong space-between custom footer), fixed-height **scroll** viewports (combobox/search 240–248, timepicker 232, filter-group list flex within fixed 360 card), Combobox-Search-Empty + Filter-Group-Empty use **text-only `EmptyState size="desktop"`** (`showIcon={false}`), Combobox-Create footer = full-width left-aligned ghost **"Add …" with no plus icon**, Combobox-Create-Empty = info `NotificationBanner` (single line) + ghost Add, Actions = dropdown-basic w/ leading icons + divider + red destructive, User Menu = avatar+name+**email** + 3 icon groups w/ dividers (Change Theme has trailing chevron), Sort direction rows have **leading `Up-Arrow`/`Down-Arrow`** + active check, Basic Filter footer = "**N** results" (bold count) + Secondary **"Close"** + Primary **"Show Results"**, Filter Group footer = Secondary **"Reset"** + Primary, two-pane left = **`type="filter-group"` rows** (active shows the count chip; × clears), filtering wired via `onSelectionChange(count)` (footer "N results" is the host's consumer-driven count, default placeholder "30 results"). **Human-readable descriptions** rewritten in both code JSDoc and Figma (`figma_set_description` on all 4 nodes). typecheck clean; dist rebuilt. **⚠ Figma Library is dirty (descriptions written via bridge) → user must publish.**
    - **✅ Review round 2 DONE (2026-06-13):** (S1) Filter Group active-selected row was clipping the "N selected" chip — rows were compressed by `flex-shrink` in the column; added **`flexShrink: 0`** to the `DesktopMenuOptions` + `MobileMenuOptions` root containers so rows keep natural height (active row now 76px, chip sits inside with 10px below). (S2) **EmptyState desktop heading `text-heading-m-semibold` → `text-heading-s-semibold`** (Figma `7339:29150` now `Heading/S/SemiBold` 20px desktop / `Body/L/SemiBold` 16px mobile — note `heading-s` is responsive: 20px ≥768px, 16px below) — fixes the oversized "No Matching Results" in Combobox-Search-Empty & Filter-Group-Empty (they already use `showIcon={false}` = the new "No Results (No Icon) Desktop" variant); added `NoResultsNoIcon` stories. (S3) **Search × now appears while typing** — `SearchField` tracks the live value (internal state when uncontrolled) and shows the × whenever the field has text (or `showClear`), no longer gated on `onClear`; clicking it clears + refocuses + fires `onClear`. Documented in `SearchField` + all four search components. (S4) **Search/Map search stories regrouped** under `Molecules/Form Controls/Search Input/{Desktop,Mobile}` and `…/Map Search Input/{Desktop,Mobile}` (one representative story per leaf); `preview.tsx` storySort updated. (S5) **User Menu avatar `tone="teal"`** (Avatar already exposes `tone: teal|warning|grey`). typecheck clean; dist rebuilt.
    - **✅ Review round 3 DONE (2026-06-13):** (R1) **Menu-option hover verified correct** — live-bridge check confirms my hover `#f5f5f5` exactly matches Figma's Combobox/Dropdown-Basic Hover fills (idle `#fefefe`, hover `#f5f5f5`, pressed `#efefef`); hover fires for every row type in Storybook. Added a `transition: background-color 120ms ease` to the `DesktopMenuOptions` row for perceptibility (if the user still sees no hover it's a stale Storybook bundle — hard-refresh). (R2) **Combobox-Create + Combobox-Create-Empty footer button** re-added the leading **`Add` (+) icon** — the designer updated it to `Ghost / Leading Icon` (was no-icon last round). (R3) **Stacked-List demarcation fixed** — removed the per-row `padding` wrapper; the container now has pad 8 + gap 8 so the full-width dividers align with the rows (Figma: 464px dividers, not edge-to-edge). (R4) **Filter Group / Filter-Group-Empty left pane** was 229px (width 196 + 32 padding, no border-box) which compressed the right pane and clipped the empty state — added **`boxSizing: 'border-box'` + `flexShrink: 0`** to the left pane (now 196px total / 163px rows) and `minWidth:0 + boxSizing` to the right pane (284px); empty-state heading now fits within the card. typecheck clean; dist rebuilt. **Full handoff: `~/.claude/plans/attach-happy-mochi.md` + `~/.claude/plans/read-the-resume-section-effervescent-naur.md`.**
    - **✅ Cards folder + Content Cards DONE (2026-06-13):** New **`Molecules/Cards`** Storybook folder groups all four card molecules — retitled `Option Card`, `Configuration Card`, `Metric Card` (was `DesktopMetricCard`) and added the new `Content Card`; storySort order set in `preview.tsx` (Option → Configuration → Content → Metric). Built **Content Cards** (`packages/components/src/ContentCard/`) — the last unbuilt molecule: **`ContentCard`** (`9896:25095`; bordered white card — `--surface-neutral-bg-primary`, 1px `--border-neutral-default`, `--rounding-xl`, `--padding-20px`, no shadow — wrapping the Figma `Container` SLOT as a `children` ReactNode in a vertical 16px-gap column; default children = a Utility `ContentPrimitives` with title/subtext + a trailing Secondary "Dispatch" button) and **`StackedCards`** (`2794:28551`; stacks Content Cards with `--spacing-16px` gap, no surface of its own). Exported both from `index.ts`; stories `Molecules/Cards/Content Card` = Default + Stacked. Parity `9896:25095` = 77/100, 0 critical, 0 real visual/spacing/typography majors (the 2 visual "majors" are token-name-vs-hex artifacts — `--surface-neutral-bg-primary`/`--border-neutral-default` resolve to the exact Figma `#FEFEFE`/`#E3E3E3`; width 824-vs-100% is fixed-vs-fill; slot diffs are React-children-vs-SLOT) — gate Pass. typecheck clean; dist rebuilt.
    - **✅ Option Card hover fixed (2026-06-13):** the Hover state was visually identical to Idle because the card surface/border tokens don't change between idle↔hover (all white fill; `--border-neutral-card-hover` == `--border-neutral-card-idle` == neutral-200). Live-bridge check of Figma `9894:18459` Hover (`7015:25812`) confirmed the **only** thing that changes on hover is a **drop shadow ("Neutral Drop Shadow 1" = `--shadow-neutral-1`)** — Idle/Pressed/Active/Focus have none. Added a `SHADOW` map to `OptionCard` (hover → `--shadow-neutral-1`, else none) appended to the inset-border `boxShadow`, plus a `Hover` story (`state="hover"`). Matches the Metric Card hover pattern. typecheck clean; dist rebuilt.
    - **✅ Grouped Input + Date & Time Pickers DONE (2026-06-13):** **Grouped Input** (`packages/components/src/GroupedInput/`, `Molecules/Form Controls/Grouped Input`, Figma `5146:845211`) — `variant` = `multi-field` (row of `InputField`s + optional trailing `TrailingInputFieldElement` dropdown), `range-input` (two fields + `Arrow-Right`), `time-input-stepper` (editable Plain-button label + Hours/Minutes/Seconds `StepperInput`s with Hr/Min/Sec units); `showHelperText`/`showTailComponent`/`showEditableLabel`. Composed fields pass `style={{width:'100%'}}` to override `FieldChrome`'s fixed `width:350` so they fill their flex wrapper. **Date & Time Pickers** (`packages/components/src/DateTimePicker/`, `Molecules/Date & Time Pickers/{Desktop,Mobile}`, Figma `1182:17579`) — **functional** (real `dateUtils` month math, weekday-aligned Monday-first grids, prev/next month+year nav, single-date / range / time selection). `Calendar.tsx` core (Month&Year header — Secondary Icon-Only `Backward`/`Chevron-Left`/`Chevron-Right`/`Forward` on desktop, Chevron-only on mobile — + Mon–Sun row + weeks grid reusing `DesktopMenuOptions`/`MobileMenuOptions` day-cell; leading/trailing days muted; range start/end = active, in-between = light `--surface-neutral-day-cell-hover` band). `DateTimePicker.tsx` orchestrator: `type` (simple/date-range/date-time) × `platform` (desktop/mobile). Date Range = preset list (Today/Yesterday/Last 7 Days/Last 30 Days/Custom combobox rows) + Start/End fields + two calendars + "**N** days selected" + Clear/Apply footer. Date & Time = calendar + demarcator + scrollable `time-picker` Time-of-Day column. Mobile = inline bottom-sheet header (title/subtitle/Ghost close ×) + single-column content + Clear/Apply pills + drag handle. **Card radius `--rounding-xxl` (16px)** per Figma (was xl/12). Parity `1467:17439` = 75/100, 0 critical (the 2 visual majors are token-name-vs-hex artifacts; radius now fixed to 16; type/platform are React-enum-vs-Figma-variant). All 6 desktop/mobile variants verified in Storybook incl. live range selection. Exported `DateTimePicker`/`Calendar`/`GroupedInput`; storySort updated (Grouped Input in Form Controls; Date & Time Pickers under Molecules). typecheck clean; dist rebuilt.
    - **✅ Date & Time Pickers fixes (2026-06-15)** — reconciled against the Figma variants (esp. Date Range): (1) **Date Range desktop layout was inverted** — the dual calendars now render in **every** state (Today / Last 30 / Custom — matching the 304-tall preset variants `1182:17575`/`4277:91689` which show preset list + calendars), and the **Start/End fields + footer appear ONLY in the Custom state** (the 476-tall `1182:17576`/`3921:84674`). Previously fields+footer showed always and calendars only for Custom — backwards. (2) **Preset selection** (Today/Yesterday/Last 7/Last 30) now fires `onApply` immediately (preset states have no Apply button in Figma). (3) **Date & Time footer** no longer shows a left summary ("No date/time selected") — Figma's footer is just Clear/Apply right-aligned. (4) **Time-of-Day list zero-pads the hour** ("01:00 AM" not "1:00 AM") per Figma. Custom-Default/Custom-Active/preset/simple/date-time all verified in Storybook vs `figma_capture_screenshot` incl. live range pick (endpoints dark + in-between band + "N days selected" + Apply enable). The real-calendar weekday alignment (vs Figma's simplified 1-under-Monday mock) is kept intentionally for functional correctness. typecheck clean; dist rebuilt.
    - **✅ Date & Time Pickers round 2 (2026-06-15)** — (1) **In-range grey band now renders** (`Calendar.tsx`): was a per-cell `backgroundColor` on the day-cell (which paints opaque white over it) using the wrong token. Rebuilt the week row as a flex row (cells 32px, gap 8) that groups consecutive in-range cells into a continuous **band `<div>`** (`--surface-neutral-bg-tertiary`, gap 8, `cornerRadius` 8 on rows containing a range endpoint else 0) — endpoints render the active dark cell, middle cells render a **transparent number** so the band shows through. `RANGE_BG` token fixed → `--surface-neutral-bg-tertiary`. (2) **Month abbreviation** (`dateUtils.ts`): added `MONTHS_ABBR` + `formatMonthYear(y,m,abbreviated)`; desktop calendars pass abbreviated ("Nov 2024"), mobile stays full ("November 2025"). (3) **"Date Time" → "Date & Time"** story display name (`name:` on both Desktop+Mobile DateTime stories). (4) **Footers now use the real `FooterFrame variant="card"` + `NotificationBanner` (neutral/subtle)** on the left + Clear/Apply right (replaces the custom footer row). **Banner-text rule** (documented in JSDoc + written to the Figma description `1182:17579`): Date & Time → "Select date & time" until BOTH date+time are picked, then "Nov 5 2024, 12:30 AM"; Date Range → "{N} days selected". (5) **NotificationBanner spacing** (`NotificationBanner.tsx`): icon↔text gap **12 → 8**, leading-icon **2px vertical padding** (Figma). FooterFrame already matched (80/88/40h, pad 20/0, gap 10) — no change. All verified in Storybook vs `figma_capture_screenshot` (band, abbreviation desktop-vs-mobile, banner messages live). typecheck clean; dist rebuilt. **⚠ Figma Library dirtied by the description write → user must publish.**
    - **✅ Date & Time Pickers round 3 (2026-06-15)** — footer banner reconciliation: (1) **Banner leading icon** — Date & Time shows the **`Calendar`** icon once BOTH date+time are picked (else the default `Info`); Date Range shows **`Calendar`** always. Added an **`icon?: IconName` override prop to `NotificationBanner`** (defaults to the type-derived icon); `DateTimePicker` passes `icon="Calendar"` for the value states. *(There is **no** outlined Calendar in the library — confirmed with the user — so the filled `Calendar` / `calendar-month-rounded` is used, matching Figma's `Icon/Calendar`.)* (2) **Banner alignment** — `NotificationBanner` icon↔text row changed `align-items: flex-start → center` (Figma Container counterAxis = CENTER); fixes the mis-aligned icon/label. (3) **Footer border** — the footer region now carries a **1.5px (`--stroke-sm`) top divider + 16px padding** (was a 1px demarcator); and the **desktop card border bumped `--stroke-xs` (1px) → `--stroke-sm` (1.5px)** to match the Figma root stroke so the card border and footer divider are consistent (mobile card stays 1px per Figma). All verified in Storybook. typecheck clean; dist rebuilt.
- **Date & Time picker footer/content structure fix (2026-06-15):** the date-time content row hugged the calendar (card collapsed to ~346px, no gap below the last row, demarcator stopped short of the footer). Per Figma (`3921:96612` → root **404h**, **Content `szV:FIXED` h332** pad **`16/16/0/16`**, Date Picker hugs at top, Demarcator + Time Picker `szV:FILL`): (1) the content row is now a **fixed `height:332` (`box-sizing:border-box`, pad `16 16 0`)** so the card = 332 + 72 footer = **404** and the calendar hugs at the top with a ~44px gap below it (for a 5-row month); (2) the **time-of-day scroll list is absolutely positioned inside a `flex:1` wrapper** so it fills the 332 height and scrolls to the footer without its (tall) content driving the row height (a naive `flex:1` blew the card up — circular sizing); (3) the **vertical Demarcator bumped `--stroke-xs`→`--stroke-sm` (1.5px)**, `alignSelf:stretch` so it reaches the footer divider (date-range vertical demarcator too; date-range keeps its own bottom-16 per its Figma frame). Card now 418×~404 (the residual ~4px is the CSS border-box vs Figma outside-stroke model). Verified in Storybook (date-time + date-range). typecheck clean; dist rebuilt.
- **Date & Time picker stroke + dimension reconciliation (2026-06-15, round 4):** the designer reverted all borders/demarcations **1.5px → 1px**, and the Date & Time card was still slightly off (calendar squeezed to 262 vs Figma 272, residual border bloat). Re-inspected `2305:19892` via bridge — root stroke **1px OUTSIDE** radius 16, **Content 418×332** pad `16/16/0/16` gap 12 with **Date Picker 272**, vertical **Demarcator 1px**, **Time Picker 90**, **Footer 72** (top stroke 1px INSIDE). Fixes: (1) desktop card border `--stroke-sm` (1.5) → a **`box-shadow` 1px ring** (`0 0 0 var(--stroke-xs)` + drop shadow) to mirror Figma's OUTSIDE stroke and reclaim the 2px a border-box border was eating — inner content area now the full 418/304/764. (2) vertical **Demarcator `--stroke-sm`→`--stroke-xs`** (1px). (3) footer top divider `borderTop --stroke-sm` → **inset `box-shadow var(--stroke-xs)`** so it draws the 1px line without adding to the 72px footer height. (4) Date & Time **calendar wrapper `flex:1`→fixed `width:272 flexShrink:0`** (was collapsing to 262 → day-grid misalignment); **time column `width:96`→`flex:1`** (≈89) so it fills the rest. (5) month label + "Time of Day" title `text-body-l-semibold` → **`text-label-l-semibold`** (Figma both `Label/L/SemiBold` 16px); title `whiteSpace:nowrap`. DOM now measures card **418×404**, content 418×332 pad 16/16/0 gap 12, cols 272/1/89, footer 72 — byte-for-byte vs Figma. Simple (304×304) + Date Range (764) verified no regression; mobile untouched (already 1px). typecheck clean; dist rebuilt.
- **Date & Time picker — height reduced + weekday-row height corrected (2026-06-15):** the designer shrank the Date & Time card so the gap below the last week is **16px** (was rendering 32). Re-inspected `2305:19892`: root now **418×376** (was 404), **Content `szV:FIXED` h304** (was 332) pad `16/16/0/16`, Date Picker **288 FILL** (calendar content 272 → 16px gap below). Two fixes: (1) date-time content row `height 332 → 304`. (2) **root cause** — the weekday ("Days of the Week") row rendered at 16px but Figma's frame is **32px** (16px text + `pad [8,0,8,0]`); the missing 8px top/bottom padding made the whole calendar 256 (not 272), so the gap came out 32. Added `padding: '8px 0'` to the **desktop** weekday row (mobile's Figma frame has pad 0 → kept padless via `isMobile` guard). This also corrected the **Simple** picker to its Figma **304×304** (was 288 tall). Verified: card 376, content 304, calendar 272, gap-below **16**; Simple 304×304; mobile unaffected. typecheck clean; dist rebuilt.
- **Date & Time pickers — mobile month-nav buttons use the Mobile Button (2026-06-16):** the mobile calendar's prev/next-month chevrons were rendering the **desktop** Ghost Small Icon-Only `Button` (with hover). Figma uses **Mobile Button · Secondary · Small · Icon Only** (verified `8446:27981` Month & Year → 32×32 Secondary/Small/Icon-Only). Fixed `Calendar` `navBtn`: mobile now renders `<Button variant="secondary" size="small" platform="mobile" iconOnly… />` — the touch-first mobile token set (idle/pressed only, **no hover**), pill shape, with the `--border-neutral-mobile-secondary-button-idle` 1.5px border. Desktop nav unchanged (Secondary Small Icon-Only, desktop). typecheck clean; dist rebuilt.
- **Date & Time pickers — continuous mobile range band + editable range fields (2026-06-16):** (1) **Mobile range band made continuous** (like desktop) — the mobile calendar grid was rebuilt from `space-between` + per-cell highlight squares (gaps between in-between days) to **7 equal-flex columns** (`mobileCol`, fills the 328px width) where the in-range highlight is a **per-column background that touches its neighbours** → one continuous band; endpoints get a HALF band (from the cell centre outward) so it connects into the dark endpoint with no stray nub, and the band reaches the row edges so it reads continuous across week breaks. Desktop grid unchanged. (2) **Range Start/End fields are now editable** on BOTH desktop + mobile — `readOnly` removed; they hold local text state (`startText`/`endText`) synced from the range, and a typed `dd/mm/yyyy` value is parsed (`parseDMY`) and committed on **blur / Enter** (invalid input reverts; commit also swaps start/end if reversed and navigates the calendar to the typed month). Verified: mobile band continuous across a week break (11→18), desktop band (10→20), and typing a date into a field updates the selection + calendar. typecheck clean; dist rebuilt.
- **Date & Time pickers — mobile rebuilt properly + adjacent-month days hidden (2026-06-16):** the mobile variants were a lightweight stub; rebuilt all three (`8446:27981/27986/27984`) against the Figma Mobile Modal structure. (1) **Header** pad `[24,16,16,16]` + bottom divider; title (`heading-s-semibold`) + optional subtitle (`body-m-regular`) on the left, **circular close button** on the right (40×40, `--surface-neutral-bg-tertiary` grey fill, 16px `Cancel` icon `--icons-neutral-default`) — was a transparent ghost `<Button>` with the wrong look. (2) **Fixed bottom-sheet heights** (Simple 500 / Date Range 568 / Date & Time 632) with the middle **content scrolling** (pad `[16,16,40,16]`, gap 20). (3) **Section headings** "Date"/"Time" (`label-l-semibold`) added for Date & Time. (4) **Time of Day is a 3-column wheel** (Hours 1–12 · Minutes 00–59 · AM/PM — `WheelColumn`), centred value dark / neighbours grey, a 1.5px-bordered 40px selection slot, default **1:00 AM** (Figma) — was an `InputField`. (5) **Footer** = `Clear`+`Apply` pills (Simple is **Apply-only**, full width) + a **home-indicator** bar (108×4, `--icons-neutral-default`, in a 24px row); top divider, pad `[16,16,0,16]`. (6) **Mobile calendar fills the 328px width** — the grid rows are now `width:100% / space-between` (Figma mobile `Days` is FILL/SPACE_BETWEEN) instead of the desktop's fixed 272 left-aligned layout, which had left ~56px dead space on the right; mobile range weeks render 7 individual cells (per-cell in-range highlight) so `space-between` stays even. (7) **Adjacent-month days now render as EMPTY cells** (no muted number) on BOTH desktop + mobile — a deliberate deviation from the Figma mock (which greys them), per the user's reference examples (Booking/Quicken style); the grid keeps the empty slot for alignment. typecheck clean; dist rebuilt.
- **Date & Time picker — time-list focus ring no longer clipped (2026-06-15):** the desktop Date & Time **Time of Day** scroll list (`overflow-y:auto` → clips both axes) was cropping a focused time row's focus ring (`outline-offset 2px` + 1.5px). Fix is in the picker, **not** the menu-option focus style (kept as-is): the scroll viewport now bleeds out with `margin:'-4px -4px 0'` + `padding:'4px 4px 0'` (`box-sizing:border-box`, `inset:0`) so the **rows keep their full 89px Figma column width/position** while the 3.5px ring sits inside the viewport's 4px gutter on top/left/right (verified: 4px clearance each side, never clipped); `scrollPaddingBlock:4px` keeps keyboard-focused rows off the vertical scroll edges. **The BOTTOM is deliberately NOT bled** — bleeding it 4px past the content boundary (332) made a partially-scrolled time row poke through the **transparent** footer divider into the footer (the "red/odd gap between the time picker and footer" the user reported); keeping the viewport bottom flush at 332 clips the last row cleanly at the divider, and `scroll-padding` still gives keyboard-focused bottom rows ring room. Day cells were verified **not** clipped (16px inset in the card → 337px clearance to the card edge), so no change there. Verified in Storybook (focused time row + Sunday day cell both show a complete ring; scrolled time list no longer bleeds through the footer divider). typecheck clean; dist rebuilt.
- **SLOT-frame audit + Breadcrumbs slot (2026-06-15):** the user converted regular frames → `SLOT` frames across components. Audited every built component's current Figma slots vs the React API: **already exposed as ReactNode props (no change)** — ContentPrimitives (`passiveElements`/`interactiveElements`) and everything composing it (Content/Option/Configuration/Metric Cards, Desktop/Mobile Menu Options, Desktop Dropdowns, Data Entry, Stacked Cards), plus Footer Frame (`leading`/children), Notification Banners (children), Empty State (`children`), Cell (5 slots), Modal Headers (`secondaryLeading`/`secondaryTrailing`/`tabs`/`breadcrumb`), Tooltip (`details`/`cta`). **Data-driven containers kept as-is** (per user) — Desktop Segment Control / Page Tabs Control / Top Filter Section drive their slot via typed `tabs[]`/`filters[]` arrays (the React equivalent of the SLOT). **Wired the genuine gap — `Breadcrumbs`** (`9108:33624`): each variant's trail-content frame is now a SLOT (`Nested Content` / `Global Client Content` / `Global Admin Content`, HORIZONTAL gap 8). Added a **`children` content-slot prop** that replaces the data-driven trail (laid out in the component's owned `--spacing-8px` row); `current` made optional (ignored when `children` given). New story `Molecules/Breadcrumbs/Content Slot (children)` injects custom crumbs; data-driven default verified unaffected. typecheck clean; dist rebuilt. *(The baseline `components.json` doesn't record SLOT nodes, so the audit was current-Figma-slots vs React-API, not a baseline diff.)*
- **Calendar alignment + Notification Banner restructure + `--padding-1px` token (2026-06-15):** (1) **Sunday-column drift fixed** (`Calendar.tsx`) — the weekday-header spans + muted/middle day cells were `flex-shrink:1` so they shrank sub-pixel (31.7px) while the `DesktopMenuOptions` day-cells held 32px (they're `flex-shrink:0`), accumulating ~2px drift by the Sunday column. Added **`flexShrink:0`** to the header spans, muted out-of-month spans, middle in-range buttons, and the range-band `<div>` → all columns now align (verified: header + day cells at identical x, 40px steps). (2) **New `--padding-1px` token** — the designer added `Padding/1px` (Alias `10404:77259` → Brand `Scale/25`=1) to the variable collections; added the `Padding/1px` entry to `packages/cli/snapshots/alias.json` (the `Scale/25`/`Scale/0` Brand primitives already existed), `pnpm tokens:generate` → `--padding-1px: var(--scale-25)` (1px) in `tokens.css` + tailwind preset; `tokens:check` clean. (3) **Notification Banner restructure** (`NotificationBanner.tsx`, Figma `3811:65015`) reconciled to the cleaned-up structure: flattened root (the root IS the horizontal `gap-16` row — `Leading element` + optional `Dismiss`, top-aligned — no more column wrapper); `Leading element` = icon + `Content` (V, gap 12 filled / 8 subtle). **Leading-icon corrected** — reverted the wrong `align-items:center`/`2px` from the prior round to **top-aligned (`flex-start`) + `paddingTop: var(--padding-1px)` (1px)** per the designer's note (sits top-left, optically centres with the first text line — the title for filled, the body for subtle). All verified in Storybook. typecheck clean; dist rebuilt.
    - **✅ File Upload Card added to Cards folder (2026-06-13):** the existing `FileUploadCard` (`packages/components/src/FileUpload/FileUploadCard.tsx`, used by the File Upload form control) got its own story `Molecules/Cards/File Upload Card` (Uploading + Uploaded) and a `File Upload Card` entry in the `preview.tsx` Cards storySort. Verified the component already matches Figma `7359:18098` byte-for-byte (2 variants: Uploading 350×92 / Uploaded 350×64; `--surface-neutral-bg-tertiary` #efefef card, `--rounding-lg`, pad 12, gap 8/row-gap 20; leading `Loading` / `Check-Circle`-outlined 20px glyph; `label-m-semibold` filename + `label-s-regular` size; Ghost Icon-Only Small `Cancel` remove ×; `DesktopProgressIndicator variant="upload"` while uploading) — no component change needed, so no dist rebuild. The **`Molecules/Cards`** folder now groups all five: Option / Configuration / Content / Metric / File Upload Card.
  - **Figma node IDs:** Desktop Menu Options `1531:5056` · Desktop Dropdowns `8230:26475` · Mobile Menu Options `8338:35033` · Mobile Dropdown `8338:35339`.

- **✅ Table Cells folder — Cell + Duration Labels (2026-06-14):** new **`Molecules/Table Cells`** Storybook folder (storySort `['Cell', 'Duration Labels']` after Date & Time Pickers). Built two components:
    - **`DurationLabel`** (`packages/components/src/DurationLabel/`, Figma `4445:107943`) — compact inline `<span role="img">`; `variant` (finished/active) × `status` (on-target/delayed/at-risk) × `time` text. **Finished** = leading status icon + greyed time (`--text-disabled-label`): on-target `Check-Circle`/`--icons-success-default`, delayed `Cancel-Circle`/`--icons-error-default` (no At-Risk variant — renders greyed time, no icon). **Active** = color-coded time, no icon: on-target `--text-default-label`, at-risk `--text-warning-label`, delayed `--text-error-label`. All `text-label-m-medium`, gap 8. Stories Finished/Active/Catalog. Parity `4445:107943` = 64/100, 0 critical/major in visual/spacing/typography (gap·height·typography·fills all aligned; the 1 critical + 1 major are `targetSize` COMPONENT_SET-chrome + non-interactive-touch-target false-positives) — gate Pass.
    - **`Cell`** (`packages/components/src/Cell/`, Figma `4444:45000`) — ONE component, `type` prop = **18 Types** (header · header-checkbox · sample · date · text-link · status · default-checkbox · actions · preview-chips · duration · select-field · item-stepper · time-stepper · list-item · address-cell · manual-order · automatic-order · driver-cell) × **4 States** (idle/hover/pressed/selected), mirroring the **DesktopMenuOptions** pattern (runtime hover/press + caller `selected` + `state` override for catalogs). Root chrome: fixed height (header 40 / body 72 / time-stepper auto), no border, vertical-centered Content frame with pad **10/12** (Status pad-right 20). State bg = `--surface-neutral-table-{cell,header}-{idle,hover,pressed}` + `--surface-secondary-table-cell-selected` (headers have no selected). **The Timer type was merged into a single `duration` type by the designer (re-scanned 2026-06-14).** Composes `DurationLabel`, `Badge`, `Button`, `Checkbox`, `Chip`, `Avatar`, `Select`, `Stepper`, `GroupedInput`, `Icon`. Maps the 5 Figma SLOTs (`statusContent`/`actions`/`timeStepperContent`/`manualOrderContent`/`automaticOrderContent`) → caller-injected ReactNode props with Figma-default renders. `-Outline` Figma icons → base name + `outlined` prop (Location/Manual-Touch/Copy/Delete/Phone/Info). **Button atom extended (2026-06-14): new `iconOutlined?: boolean` prop** renders the button's icon(s) outlined (passes `outlined` to the inner `<Icon>` for iconLeft/iconRight/iconOnly; default false → no regression); the Cell's order-cell Copy buttons and the Driver-cell Phone button pass `iconOutlined` (Figma `Copy-Outline`/`Phone-Outline`) while the Swap button stays filled (Figma `Icon/Swap`). Interactive-element icon colors: Manual-Touch `--icons-caution-badge`, Calendar `--icons-information-badge`, Integration `--icons-notice-badge`. Address connector = 1.5px **solid** `--border-neutral-default`. List Item / Address / Order ID truncate with ellipsis (annotation-driven). 18 per-Type stories (states + Header icon booleans) + Catalog. Parity `4444:45000` = 52/100, **0 critical**; spacing (10/12, gap 8) + typography aligned; the 3 majors are documented artifacts — `visual/backgroundColor` reads the COMPONENT_SET frame fill `#EFEFEF` (= header-idle token, not a per-variant fill; cells bind the correct `--surface-neutral-table-*` tokens, verified in Storybook), kebab-vs-TitleCase enum naming, and the 2970×1153 set-bounds `targetSize`; the 18 info/minor are the SLOT/TEXT/INSTANCE_SWAP Figma props vs the clean React API — gate Pass.
    - **Descriptions** (human-readable, synthesized from the user's Cell annotations — right-align numerics, ellipsis truncation, order-ID max-250, interactive-elements purpose, "actions can be multiple buttons") confirmed by user and written to **both** Figma component descriptions (`figma_set_description` on `4444:45000` + `4445:107943`) **and** code JSDoc. typecheck clean; `@leta/components` dist rebuilt. **⚠ Figma Library dirtied by the description writes → user must publish.**
    - **✅ Reconciliation pass (2026-06-14, round 2)** against updated Figma + the **Grouped Input** form control: (1) **Grouped Input `time-input-stepper`** (`GroupedInput.tsx`) reconciled to Figma `5146:845211` — editable-label Plain button now `showUnderline={false}` (Figma `Show Underline:false`); the Hours/Minutes/Seconds **unit labels removed** (`showLabel={false}` — units are identified only by the trailing `Hr`/`Min`/`Sec` `TrailingInputFieldElement` suffix); each unit's discrete `<Stepper>` now **fills** via `style={{width:'100%'}}` + the `StepperInput` `stepper` slot (was rendering at `FieldChrome`'s 350px default → row blew out to ~1100px, which also threw off the cell's Add Row). (2) **Time Stepper cell** (`Cell.tsx`) — default `<GroupedInput>` now `style={{width:'100%'}}` so it fills the cell; the full-width Dashed Medium "Add Row" button now centers correctly. (3) **Date cell** — split into two colours per Figma segment fills: date line `--text-default-label`, time line `--text-default-label-idle` (split `date` on first `\n`). (4) **Header / Header Checkbox** — Figma now has **Idle only**; `surfaceVar` always returns `--surface-neutral-table-header-idle` for header types (no hover/pressed), stories show Idle only. (5) **Address cell** — connector line changed **dotted → solid** (`--border-neutral-default` 1.5px, centered under the 12px icon column) and dropoff text `--text-default-heading` → **`--text-default-label`** (pickup stays heading). All verified in Storybook (6107) vs `figma_capture_screenshot`; typecheck clean; dist rebuilt. No Figma writes this round.
    - **✅ Reconciliation pass (2026-06-14, round 3)** — further fixes after re-scanning Figma: (1) **Address pickup colour** changed `--text-default-heading` → **`--text-default-label`** (designer changed it; both pickup & dropoff now `--text-default-label`). (2) **Address connector line** rebuilt to mirror Figma's exact structure — a continuous 1.5px solid line through the **12px icon column**: a 4px stub under the pickup icon, a 12px connector, a 4px stub above the dropoff icon, all centered under the icons (was a single short 12px line between rows). (3) **Grouped Input `time-input-stepper`** — the `Hr`/`Min`/`Sec` suffixes are now **center-aligned** with the fields (`alignItems: center`, was `flex-end`/bottom). (4) **Time Stepper cell "Add Row"** button now has a **leading `Add` (+) icon** (`iconLeft="Add"`; Figma Add Row is `Type=Leading Icon` in Hover/Pressed/Selected — Idle was a designer miss). (5) **Header-checkbox / default-checkbox interactivity** — `checked` now defaults to **undefined** so the composed `<Checkbox>` is **uncontrolled and self-toggles** checked↔unchecked on click (pass a boolean to control); `HeaderCheckbox` story documents default(click-to-toggle)/checked/**indeterminate** (the some-but-not-all-rows-selected case). (6) **Date cell** verified correct — two distinct colours render (`#101010` date / `#4f4f4f` time via `--text-default-label` / `--text-default-label-idle`, confirmed by computed styles); the earlier "no change" was a stale cache. typecheck clean; dist rebuilt; all verified in Storybook (6107).
    - **✅ Reconciliation pass (2026-06-14, round 4)** — (1) **Address connector** rebuilt again as a **single continuous solid line** element (the round-3 3-segment stub+connector+stub approach rendered as separate dashes due to inter-row gaps) — one `borderLeft` div, `height:14` with `marginTop/Bottom:-4` to bridge into the icon rows so it touches both icons; verified continuous. (2) **Grouped Input editable label** — the trailing **Edit icon now renders outlined** (`iconOutlined` on the Plain Button; Figma uses `Icon/Edit-Outline`) — was rendering the filled glyph; verified the rendered SVG path matches the registry `outline` entry. typecheck clean; dist rebuilt.

- **✅ Organisms Batch 1 — Navigation + Table stack COMPLETE (9/9, 2026-06-16).** All compose existing molecules/atoms (no re-implementation); per-component story-list confirmed; typecheck-clean; dist rebuilt. Stories under `Organisms/Tables` + `Organisms/Navigation` (User-Menu is an Atom → `Atoms/User-Menu`). storySort `Organisms` block added to `preview.tsx`. **Table stack:** `DataRows` `4445:105200` (flush `Cell` row, basic 52/complex 80, state→Cell-state, hybrid `cells[]`+children); `Table` `2192:8107` (bordered card, header `Cell` row + Data Rows + `Pagination`-table footer, `columns`/`rows` shared widths, `selectable`, `maxBodyHeight` sticky scroll); `BulkActionsToolbar` `7973:17061` (floating pill, selection+summary | actions+More | close, `--shadow-neutral-3`, `fixed` placement); `TableDataControl` `7575:36637` (3 variants search-create/search-column/filters-column, slot defaults reuse SearchInput/Button/TopFilterSection); `TableContainer` `1524:40283` (default = 2 controls + Table / empty = control + `EmptyState` no-orders). **Navigation:** `Sidetab` `7751:26369` (160 col of side-tab menu options); `SideBar` `3714:35743` (expanded 240 / collapsed 72; logo+search+grouped nav+footer; reuses LetaLogo/CollapsedSidebarLogo/SearchInput/DesktopMenuOptions/Button; `Sidebar Nav Groups` `89:46481` deferred); `UserMenu` `7446:22541` (Atom: Avatar+chevron pill, 3 states, `--surface/border-neutral-user-menu-*`); `TopBar` `7519:27364` (64px header, Breadcrumbs global-admin slot + bell + UserMenu; active = red unread dot). Full per-component sign-off in PLAN.md `### Organisms — Batch 1`.
    - **✅ Corrections round 1 (2026-06-16):** **Avatar initials colour is now tone-aware** (global atom fix, affects all Avatar/User-Menu consumers) — teal/warning → `--text-default-label` (dark), grey → `--text-on-color-label` (white), per Figma `7446:22517` (was white for all; grey/driver-cell unchanged). **Side Bar** made interactive — internal active-on-click + internal collapse toggle (expanded Sidebar btn ↔ collapsed logo, uncontrolled, seeded from `variant`); full-width 1px header bottom demarcator; footer icons Help Center=`Support` / Get the Dispatch App=`Widget`; all strokes→1px; collapsed icon-only options centered (`alignItems:center` — fixed-40px options left-align under flex `stretch`). **Cell** address-connector 1.5→1px. **Top Bar** is `width:100%` (fills container; Figma 1200 = canvas only), stories full-bleed. **Bulk Actions Toolbar** border/dividers confirmed already 1px. typecheck clean; dist rebuilt.
    - **✅ Design-system consistency pass (2026-06-17):** (1) **Top Filter Section** row gap 12→8px. (2) **Desktop + Mobile Button stroke 1.5→1px** — code (`Button.tsx` LAYOUT `borderWidth` 1.5→1; focus ring stays `--stroke-sm`) **+ Figma bridge** (Desktop set `28:38245` 385 borders→1, Mobile `2887:26272` all→1; Focus-ring child nodes preserved 1.5). (3) **All borders/demarcators standardized to 1px (`--stroke-xs`)** in code — Badge/MobileBadge/Shortcut/Tag/LeadingInputFieldElement/DesktopMetricCard(pressed)/UserMenu/MapZoomControl/ModalHeaders/DateTimePicker(wheel)/MobileDropdown/CollapsedSidebarLogo/TopBar(notif-dot). **Exceptions kept 1.5px (`--stroke-sm`):** every focus ring, Desktop Chip Active, Filter-Group Active/Active-selected, Basic/Advanced Top-Filter Idle-selected, and **Option Card Active + Mobile Chip Active** (brought UP to 1.5px, now conditional on active state). (4) **Token re-sync** — fingerprint of all 5 variable collections + text/effect styles found only **Alias** + **Mapped Sizes** changed; re-pulled both, regenerated, `tokens:check` clean. **Avatar `7446:22517` reconciled: `medium` 40px (was 44)** + initials text styles → xs caption-l(10)/small label-s(12)/medium label-m(14)/large heading-m(24). Avatar consumers auto-update via the size prop. **⚠ Figma Library dirty (button-stroke writes) → user must publish.** Full deep component-diff vs the stale 2026-06-03 baseline intentionally skipped (noise).
- **✅ Organisms Batch 2 — Forms / Lists & Sections `217:4848` COMPLETE (2026-06-24):** Input Section (`10557:36087`), List Section (`217:6694`), Table Section (`4818:152180`) — all collapsible (Open/Close), Section-Heading CP headers, group-header subtitles hidden per Figma. Stories under `Organisms/Forms`.
- **Organisms — remaining:** Mobile Screens: Maps `3137:23974` / `7044:24001` / `10255:63277` (enumerate + build — compose `MapView` for the map surfaces).
- **Batch 4 remaining molecules:** Confirmation Dialog, Accordion, etc. (can be interleaved or deferred — confirm priority with user). **Resolved (2026-06-22):** the Advanced Top Filter overlay reuses the already-built `DesktopDropdowns` combobox variant — no separate component to build; there is **no "Dashboard Cards" component** (the `4239:74634` node is Desktop Metric Cards, already built).
- **✅ Templates — Modal shells COMPLETE (4/4, 2026-06-23).** Figma Templates page `55:34578` → "Modals" frame `228:12423`; all four are modal shells composing `ModalHeaders` (`showSecondaryContent={false}`) + body + `FooterFrame` over a shared internal **`Modal/ModalShell.tsx`** (root: width, radius 12/0, `--surface-neutral-bg-default`, 1px `--border-neutral-default` OUTSIDE box-shadow ring, clipped; optional fixed `bodyHeight` → scroll). Composition only — no re-implementation. **`AlertDialog`** `1106:2868` (480×256, basic/warning), **`ModalDialog`** `1317:4855` (512w; comment=TextArea / form=2-col InputField / signature+image=480×304 rect / multi-choice=OptionCard list), **`LargeModal`** `228:11413` (768w; drawers square 872 + with-tabs header + Input/List Section bodies / centered rounded 768 + Input+Table Section; body scrolls; `preference` footer), **`ExtraLargeModal`** `7900:165088` (1024×768; horizontal left Table panel + right Map panel w/ MapZoomControl). Stories `Templates/*` (storySort block in `preview.tsx`); exported; typecheck clean; dist rebuilt. Parity: AlertDialog 73/100, ExtraLargeModal 91/100 — both 0 critical, 0 visual/spacing/typography major (gate Pass); ModalDialog/LargeModal share the same shell+tokens. Full sign-off in PLAN.md `### Templates built`. **✅ Behaviour-spec docs written (2026-06-24)** for all 4 in the standard format (what it is + variants + when to use / when NOT to use) via `figma_set_description` (ExtraLargeModal's prior rough description reformatted to match); code JSDocs carry the same framing. **Reconciliation since:** drawers now full-viewport-height side-sheets (sticky header/footer, scrolling body); centered = compact Input Section + Table Section + preference footer; ExtraLargeModal table has fixed-width columns + horizontal scroll (4th column peeks) + `showPages={false}` pagination, and its map uses the new **`MapView`** (Leaflet/OSM) standard. ⚠ Figma library dirty (description writes) → publish. **Next tier: Pages.**
- **✅ Pages COMPLETE (2026-06-24)** — Figma `Page` set `9626:14490` (2 variants) → one **`Page`** component (`packages/components/src/Page/`) with `variant="data-table" | "configuration"`. Collapsed `SideBar` + `TopBar` (fixed) + scrolling body (`Title` + `PageTabsControl` + info `NotificationBanner` + variant content: `TableContainer` / `Sidetab` + `ConfigurationCard`s). Composition only. Story `Pages/Page`; behaviour-spec in Figma + JSDoc. ⚠ publish.
- **✅ Phase 3 Playground — built + hardened, ready to deploy (2026-07-04 → 2026-07-05).** `apps/playground` (`@leta/playground`) is a working Vite/React app: `Page`-style shell (`SideBar` + `TopBar`), zustand store (`mockData.ts`/`useStore.ts`), and a full **Deliveries/Orders** screen (`OrdersPage.tsx`) — filter tabs (Unassigned/Dispatched/Finished/All) with sub-status pickers, a live Table, row actions, bulk selection + toolbar, a Columns control, search/sort/filter/date-range popovers (`DesktopDropdowns`/`DateTimePicker` reused), and a `MapPage` (Leaflet/OSM via `MapView`). Full detail (including every bug-fix round) in `~/.claude/plans/playground-deploy-handoff.md`; highlights:
  - **Table Column Layout spec revision reconciled end-to-end** — Figma wireframes (Playground file, "Order Management" page) redone first, then code: `columnPresets.ts` rewritten as explicit per-instance presets (`ORDER_TABLE_COLUMNS` Actions-64, `UNASSIGNED_ORDER_COLUMNS`/`BROADCASTED_ORDER_COLUMNS`/`SCHEDULED_ORDER_COLUMNS` Actions-154 + optional Batch ID, `ALL_ORDER_COLUMNS` no checkbox/actions), sticky-pin capability added to `Table`/`DataRows` (Order ID left / Actions right, `pinned` field). Skill docs (`table-column-layout/SKILL.md` + `references/spec.md`) fully reconciled — see there for the authoritative spec, including the new **§4.4 wide-viewport proportional scaling** (`Table` derives each column's *design width* at the 1320px canvas via `computeDesignWidths` and switches to a scaled model — not raw surplus-dumping — once the container exceeds it, fixing dead space in sparse instances like *All*) and the **Modal/embedded-tables rule** (reduced column set, same weighting logic, own width).
  - **Table scroll model overhauled** after playground QA, then **corrected to measured `scrollX="auto"` (2026-07-07)**: `Table`'s `scrollX` prop accepts `boolean | 'auto'`; `'auto'` computes the table **minimum width** (checkbox + fixed widths + flexible floors) and flips into scroll mode **only while the container is narrower than that minimum** (spec §4.3's exact rule — column-set changes re-measure synchronously, window resizes via ResizeObserver). The playground passes `scrollX="auto"`, so optional columns just join the flex-fill on sparse views (the earlier approximation forced scroll whenever ANY optional column was on, which wrongly h-scrolled the roomy Returned view). When active, a single both-axis scroller keeps Order-ID-left/Actions-right pinning correct; the **vertical** scrollbar chrome is always hidden so pagination reads as the boundary, while the **horizontal** bar stays visible as the real overflow cue (since 2026-07-07: a 6px rounded pill inset 16px from each side) — no phantom bars either axis. Pinned anchors carry soft 14px gradient edge-fades (hidden at scroll extremes) instead of hard shadows; the fades sit inside the cells so they never overlap the scrollbar.
  - **Bulk-actions "N selected" popover** (`apps/playground/src/components/Popover.tsx`) gained `top-start`/`top-end` placements that anchor by **bottom edge** (grows/shrinks upward, stays glued to the toolbar — no growing gap on deselect) + a `ResizeObserver`; its order-ID rows truncate (`SelectionControl` gained a `fullWidth` truncating-label mode, wired through `DesktopMenuOptions`); the list height is tuned so the 5th row is deliberately cut off (scroll affordance) with the scrollbar chrome hidden (`.leta-popover-list`).
  - **Sliding active-filter border** — `TopFilterSection` gained an opt-in `animatedSelection` prop: a floating ring (not the pill's own border) slides between filters with `300ms cubic-bezier(0.2,0,0,1)` easing, engaging only once fonts have settled and only on a real selection change (no phantom "settle" slide on load); `TopFilter` gained `showSelectedBorder` so the pill doesn't double-paint. The playground's status filter row opts in.
  - **Toast enter/exit choreography fixed** (`apps/playground/src/shell/AppShell.tsx` `ToastRegion`) — dismissal (auto-timeout or ×) now marks the toast `exiting` and defers the store removal until the `leta-toast-out` keyframe finishes, mirroring the `Popover`/`BulkActionsToolbar` exit pattern; previously toasts vanished instantly with no exit animation.
  - Note found in passing (unrelated, already documented as pre-existing): `Button`'s `:active` state applies `transform: scale(0.96)` — matches the `make-interfaces-feel-better` skill's "Scale on Press" principle, but predates this session's work; not something added here.
- **✅ Deployment COMPLETE (2026-07-05).** Repo pushed to `github.com/ifeanyi-99/LETA-Playground` (force-pushed over GitHub's auto-generated initial commit, per user confirmation — local history had the real project). Vercel project `leta-playground-playground` (team **LETA**) imported with Root Directory at the repo root, Framework Preset **Vite**, and `vercel.json`'s explicit `installCommand`/`buildCommand`/`outputDirectory` driving the build. **First deploy failed** — real bug, not a fluke: `packages/design-tokens/package.json`'s `build` script only ran `tsup` (index.js/fonts.js) + copied `scroll-utilities.css`; `tokens.css`/`text-styles.css`/`tailwind.preset.cjs` are produced by the separate `tokens:generate` step, which nobody wired into the package's own `build`. Since `dist/` is gitignored, those files only existed on dev machines from prior manual `tokens:generate` runs — a fresh checkout (Vercel) had no `dist/` at all, so Vite failed to resolve `@leta/design-tokens/css`. **Fix:** `design-tokens`'s `build` script now also runs `pnpm --filter @leta/cli tokens:generate` (reads only the committed `packages/cli/snapshots/*.json` — no Figma/network access needed in CI). Verified by wiping all package `dist/` output locally and rerunning `pnpm turbo run build --filter=@leta/playground...` clean before pushing the fix. Redeployed successfully — **live app verified**: Deliveries/Orders table renders correctly (filters, columns, pagination), client-side routing survives a hard refresh (SPA rewrite works), and the Map page's Leaflet/OSM tiles load. Follow-ups same day: **route-level code-splitting** (`App.tsx` `React.lazy` for OrdersPage/MapPage — Leaflet now loads only on `/map`; main chunk 1750→1389 KB min / 628→519 KB gzip; the rest is the monolithic `@leta/components` dist, splitting that is out of scope) and **Storybook deployed as a second Vercel project** (`leta-playground-storybook`, same repo). **Key gotcha:** a `vercel.json` that defines install/build/output **locks those dashboard fields read-only** (vercel.json > dashboard, not the other way) — so a second project on the same repo can't just override them in the UI. The working pattern: Storybook project sets **Root Directory = `apps/storybook`**, where Vercel picks up the committed `apps/storybook/vercel.json` (`cd ../..` commands so pnpm/turbo see the workspace; `outputDirectory: storybook-static` relative to the root dir). The import screen still previews the repo-root vercel.json values (stale UI) — actual builds resolve the Root-Directory one. Full history in `~/.claude/plans/playground-deploy-handoff.md`.
- **✅ Table-column investigation RESOLVED (2026-07-05):** the wide-monitor Order ID truncation was **not a `Table.tsx` bug** — `computeDesignWidths`/§4.4 scaling worked exactly as spec'd; the spec's own 224px cap was the cause (long mock IDs + a hard ceiling = ellipsis beside free space). Per user decision, **the cap was removed**: Order ID is now a **low-weight flexible** identifier (min 150 / flex 0.5 / no `maxWidth`) — the low weight is the governor (Route still gains ~3× per surplus px). Updated: `columnPresets.ts`, spec §3.3 (retitled "Low-weight flexible identifiers") + §4.4 + instance tables, `SKILL.md`. Verified: 2200px viewport → Order ID 333px, 0 truncated IDs; 1280px → 179px, Route still widest. **Orders search wired same session:** toolbar placeholder now "Search orders by ID or recipient"; live filtering by Order ID / recipient name / phone (digits-only compare, local `07xx` ↔ `+254 7xx` normalized). `TableDataControl` gained `searchValue`/`onSearchChange`/`onSearchClear` (threaded to `SearchInput`); `OrdersPage` filters within the active status view, resets to page 1 on query change, keeps ID-based selection. (2) still queued: the user's **order management flows** document to drive the next round of playground screens. Context: `~/.claude/plans/playground-flows-handoff.md`.
- **✅ Table refresh + Loading Overlay + Table Container No Results (2026-07-06):** reconciled two Figma updates + shipped the loader. (1) **Table Data Control `7575:36637`** gained a **Refresh** icon-only button (Secondary/Medium, filled `Icon/Refresh`) **after** the Columns button in the Column Control & Count group — `TableDataControl` adds it + a new `onRefreshClick` prop; group gaps reconciled to Figma (outer 8, count-text↔divider inner 12). (2) **Table Container `1524:40283`** gained a **`no-results`** variant (`10768:20467`) — both toolbars stay + centered `EmptyState type="no-results"` with description override "Try adjusting your filters & date."; new `noResults` slot prop; story `Organisms/Tables/Table Container/No Results`. (3) **New `LoadingOverlay`** (`packages/components/src/LoadingOverlay/`, story `Molecules/Loading Overlay`) — blocking loader, **reworked 2026-07-06 pm after user feedback**: **`contained` prop** (absolute inset-0 over the nearest `position:relative` region — the table region — vs default fixed viewport), **light scrim `rgba(254,254,254,0.85)`** (white @85%, matches the live LETA console; no scrim token exists), the **LETA loader Lottie at 80×80** (canvas renderer; `lottie-web` externalized in tsup like leaflet, dynamic import; the 1.6MB JSON ships as a dist asset — `src/LoadingOverlay/assets/leta-loader.json` → `dist/assets/`, fetched via `new URL(import.meta.url)`; **engine + JSON preload at MOUNT via module-level cached promises** — first-open used to start the animation ~1.5s late), then 20px below two stacked lines gap-8: "Loading..." (`text-heading-s-semibold`/`--text-default-heading`) + "Wait a few seconds." (`text-body-m-regular`/`--text-default-sub-body`); `role="status"` aria-live. **Cycle-aligned dismissal**: `open=false` doesn't hide until the animation finishes its full ~3.7s cycle (121f @33fps) — `loopComplete` listener + a remaining-cycle-time fallback `setTimeout` (rAF suspends in hidden tabs → `loopComplete` alone would hang). Playground **empty-state-first flow** (wireframes `1176:171818` / `945:210612` in the Playground Figma, both = `Table-Container` instances): Orders boots into `TableContainer variant="empty"` (Search+Create toolbar only, No Orders Yet + illustration CTA); **Add Order (the empty-state illustration CTA — `handleAddOrder`, the future side-drawer seam) or Refresh** → contained overlay (top edge extends up through the page column's 24px gap to sit **1px below the Page Tabs control**, `style top: calc(-1*var(--spacing-24px)+1px)`) → populated table; once loaded, **zero-match search/filters → `variant="no-results"`** (both toolbars stay). **(2026-07-07)** Per updated Figma `4368:154614`, the empty-state toolbar drops the Primary **Add Order** button beside Import/Export (the illustration CTA is the single primary action) — new `TableDataControl` prop **`showAddOrder`** (default true) hides it; `TableContainer variant="empty"` passes `showAddOrder={false}` and so does the playground's empty-state toolbar. The populated + no-results toolbars keep Add Order. All verified in playground (5181) + Storybook (6108); no Figma writes (library clean). `.claude/launch.json` has `storybook-alt`/`playground-alt` configs (6108/5181) for when another session holds the primary ports.
- Phases 4–6 Figma generation skills — **deferred to a separate session**

See [PLAN.md](PLAN.md) for the full breakdown, `~/.claude/plans/organisms-build-handoff.md` for the Organisms/Templates/Pages resume context, `~/.claude/plans/playground-deploy-handoff.md` for the Playground build + deployment resume context, and `~/.claude/plans/playground-flows-handoff.md` for the current resume point (table-column investigation + interactive flows).

## Focus state conventions

All interactive components use the same `:focus-visible` treatment:

```css
outline: var(--stroke-sm) solid var(--border-secondary-component-focus);
outline-offset: 4px;
```

`--stroke-sm` = 1.5px; focus color = yankees-blue-900 (light) / yankees-blue-50 (dark). Mobile Button currently shares this with Desktop in code — the proper Mobile Focus variant is pending in Figma, parity will be re-checked once that's added.

## Map views — standard map primitive (set 2026-06-24)

**Every map view in the system renders through `MapView`** (`packages/components/src/MapView/MapView.tsx`) — the canonical interactive map primitive. Do **not** hand-roll maps (no OSM `<iframe>` embeds, no static screenshots) in new components; compose `<MapView>` and override `center`/`zoom`/`tileUrl` as needed.

- **Engine:** [Leaflet](https://leafletjs.com/) (`leaflet` dependency, `@types/leaflet` dev) with **OpenStreetMap** raster tiles — open-source, **no API key**. Leaflet is loaded client-side via dynamic `import('leaflet')` (SSR-safe) and externalized in the tsup build (`--external leaflet`); its CSS is injected at runtime (CDN-pinned, matching the runtime-style-injection convention — no build-time CSS import). Requires network access for tiles.
- **Zoom:** Leaflet's built-in zoom control is disabled (`zoomControl: false`); the design-system `<MapZoomControl>` is overlaid bottom-right and wired to `map.zoomIn()` / `map.zoomOut()`, disabling at min/max zoom.
- **This is interim.** It stays the standard **until a more robust provider (e.g. Google Maps) is adopted** — when that happens, swap the internals of `MapView` only and every consumer (Extra Large Modal, future map screens) updates automatically. Keep the public `MapView` API provider-agnostic.

## Figma a11y documentation (written 2026-05-10)

Seven components now carry Accessibility documentation in Figma — both as an `## Accessibility` section in the component description and as pinned annotations under the Accessibility / Interaction / Development categories. Components covered:

- Toggle (`1956:29506`)
- Checkbox (`4445:96840`)
- Radio Button (`9752:20357`)
- Leta Logo (`8398:6917`)
- Desktop Button (`28:38245`)
- Mobile Button (`2887:26272`)
- Collapsed Sidebar Logo (`9132:34635`)

Each covers: Role, Accessible name derivation, Keyboard bindings, Focus treatment (1.5px outline / `--border-secondary-component-focus` / 4px offset), States exposed to AT, Touch-target rationale. Note: the parity tool doesn't currently read description/annotation content for scoring, so the existing `accessibility / role` and `accessibility / keyboardInteractions` info-level deductions remain in parity reports — but the docs are now authoritative for Dev Mode handoff.

## Open issues to confirm with user (flagged during Button inspection)

These are minor Figma quirks that the Button code mirrors exactly. None blocks shipping — but worth a designer review at some point:

1. **Small Icon-Only Primary** uses Idle tokens for Hover/Pressed/Focus (only Disabled differs). Intentional static-accent treatment, or a remaining spec gap?
2. **Mobile Ghost Error Idle** has surface/border tokens; **Desktop Ghost Error Idle** is bare. Confirm intentional platform difference.
3. **Disabled border** uses `--border-disabled-button` on desktop, `--border-disabled-default` on mobile. Confirm intentional.

## Project-local skills

Skills auto-loaded by Claude Code from `.claude/skills/` in any session opened against this repo:

- **`figma-design-system-sync` (v0.3.0)** — scans the Figma library, diffs against the saved baseline, produces a markdown change report and PLAN.md update for code work. Trigger phrases: "what changed in Figma", "sync the design system", "audit visual parity". See `.claude/skills/README.md` for the full description, or `.claude/skills/figma-design-system-sync/SKILL.md` for the 6-phase workflow.
- **`table-column-layout`** — the Table Column Layout Specification (a System of rules + per-table Instance presets) for every data table. **Never make all table columns equal width**: classify each column (Primary / Identifier / Secondary / Utility / Control), fixed `width` for non-primary, weighted `flex` for primary (Recipient = 1.00 base → Route 1.48 / Driver 1.22 / Recipient 1.00), a `minWidth` floor per flexible column, and Order ID as a **low-weight flexible** identifier (min 150, flex 0.5, **no cap** — the former 224px `maxWidth` was removed 2026-07-05 after wide-monitor review; the low weight is the governor). The Control/Actions column has no visible header but an `accessibleName` (SR-only), and **Actions is instance-specific**: `ACTIONS_OVERFLOW` = 64px (overflow-only, Order table) vs `ACTIONS_DISPATCH` = 154px (Dispatch + overflow, Unassigned views). Reuse the presets from `@leta/components` — one per Figma wireframe instance, keyed to the order's lifecycle stage: **`ORDER_TABLE_COLUMNS`** (full — Order ID · Trip · Driver · Route · Recipient · Duration · Created · Status · Actions-64; dispatched & finished) · **`UNASSIGNED_ORDER_COLUMNS`** (Pending — no Driver/Trip; Route/Recipient auto-settle 60/40; Actions-154) · **`BROADCASTED_ORDER_COLUMNS`** (Pending set **+ Batch ID 90** after Order ID) · **`SCHEDULED_ORDER_COLUMNS`** (Pending set **− Duration**, no Batch — pre-queue) · **`ALL_ORDER_COLUMNS`** (search/triage — Order ID · Route · Recipient · Duration · Created · Status; **no** Checkbox/Driver/Trip/Batch/Actions; use `<Table selectable={false}>`). Plus `LAST_UPDATED_COLUMN` (hidden-by-default toggle, splices **between Created and Status**). Order ID is `pinned: 'left'` and Actions `pinned: 'right'` — sticky anchors on horizontal scroll (spec §4.3), a **no-op unless scroll mode is active** (the playground passes `scrollX="auto"` — scroll engages only while the column minimums exceed the container, per §4.3; optional columns on a roomy view just join the flex-fill; table scrollbar chrome: vertical always hidden, horizontal a 6px inset pill in scroll mode only; pinned-edge gradient fades hide at the scroll extremes). The checkbox column comes from `<Table selectable>`. **Sizing model (spec §4.1 + §4.4, 2026-07-04):** flexible columns render `flex: <weight> 1 <floor>px` — the **floor is the flex-basis** (floors first, only the *surplus* shared by weight; a 0 basis deviates from the design), so at the 1320 design canvas every instance renders its Figma px exactly; **beyond the design width the approved layout scales proportionally** (`Table` derives per-column design px via `computeDesignWidths` + a ResizeObserver threshold — information columns grow in design ratio and never drop below it, control columns stay fixed, Order ID scales with the rest since its 224 cap was removed 2026-07-05). **Modal / embedded tables** carry a *reduced* column set but obey the same System rules (weight-based fill, floors, Route widest, order of significance, no dead gap) at their own content width. `TableColumn`'s `role` defaults the sizing mode, `Table` dev-warns if no primary column exists, and adding a table = a new preset, not a redesign. Trigger phrases: "build/change a table", "balance the column widths", "columns too wide/truncated/squished". See `.claude/skills/table-column-layout/SKILL.md`.

### Figma baseline

The skill's baseline was last captured **2026-07-08 (full `--deep`)** against file `Kxbgc2KoJSmTxvSV3PwNEu` ("Library") — all 93 sets carry fresh deep variant data (Flags now includes the `leaves` field), so the next diff is clean. **2026-07-08 sync result:** no token/structural drift; one edit — **Notification Banners `3811:65015`**: trailing section restructured to a full-height right-aligned `space-between` column (Dismiss × top-right `layoutGrow:1`, optional **CTA** bottom-right, new `cta` prop, hidden by default) + Filled corner radius `--rounding-lg`→`--rounding-xl` (8→12). A follow-up color-variable scan added one token — **`Border/primary/default`** → `--border-primary-default` (coral-red #ff3941/#ff6167; refreshed mapped-colors + regenerated, no consumer yet). Report: `docs/figma-changes/FIGMA_CHANGES_2026-07-08.md`. **2026-07-07 sync result (implemented in-session):** Tier-1 found two additive axis changes — **Cell `4444:45000`** Type 18→20 (added **API** → new `api-cell`; **User** already built as `user-cell`) and **Table-Container `1524:40283`** gained `No Results` (already built 2026-07-06). A follow-up **`--deep`** pass then caught: **Featured Icon `8967:38281`** gained a **Teal** color (→ `notice` token family; added `teal` to `FeaturedIcon`), the **API cell** switched its Featured Icon Highlight→**Teal** (`Cell.tsx` api-cell), token **`Icons/notice/default`** #80c6be→#669e98 (mapped-colors refreshed + regenerated), and small padding fixes (**Tag** L8/R6→L6/R4, **Metric Card** 16→20 uniform, **Menu Options `dropdown-advanced`** 10→12/64→68). The other ~300 deep-diff deltas were verified as **stale-baseline** (June reconciliations already in code). Report: `docs/figma-changes/FIGMA_CHANGES_2026-07-07.md`. **Baseline shape changed 2026-07-02:** `components.json` now stores 486 top-level entries (93 sets with `variants` + 393 standalone components) — variant children are no longer duplicated as separate COMPONENT rows (the old 2431-entry shape). Baseline files (skill-managed):

- `packages/cli/snapshots/components.json` (2431 components, 93 sets with variants carried forward; catalog refreshed 2026-06-28)
- `packages/cli/snapshots/annotations.json` (9 annotated components)
- `packages/cli/snapshots/descriptions.json` (60 components with prose descriptions — includes the 7 molecule descriptions written 2026-06-22: Grouped Input, Stacked Cards, Content Card, File Upload Cards, Top Filter, Page Tabs Control, Top Filter Section)

**2026-07-03 sync result (implemented in-session):** Content Primitives `6961:41406` **Metrics variant** — the `Metric` frame flipped horizontal→vertical: the variance Badge now stacks **below** the metric value (left-aligned), not beside it (variant height 108→136). `MetricsLayout` in `ContentPrimitives.tsx` → `flexDirection: column`; consumers (Metric Card) inherit it. No token/catalog changes; read-only scan (nothing dirtied). Report: `docs/figma-changes/FIGMA_CHANGES_2026-07-03.md`.

**2026-07-02 sync result (all implemented in-session):** (1) new **`Icon/Redo`** (`10736:25670`, key `a4e2873b…`, glyph `redo-rounded`, no outline → registry `outline:null`) — inventory + `pnpm registry` (**258 semantic names**) + icons dist rebuilt + appended to the `preferredValues` of all 50 icon instance-swap slots across 28 sets (⚠ library dirtied → publish). (2) **Content Primitives `6961:41406`** — Metrics: metric value `Heading/S/SemiBold` (was M; height 112→108) + description `Label/S/Regular` (was Body/S) → code classes updated; **Section Heading gained a Visual Anchor** (20px outlined icon, centered vs title+subtext, defaults true) → `hasVisualAnchor` includes section-heading + `VisualAnchor align` prop; all consuming organisms (Input/List/Table Section, XL Modal) confirmed `Show Visual Anchor: true` in Figma and inherit correctly. (3) Table Data Control 3→5 variants (Active Filter) — already in code. Report: `docs/figma-changes/FIGMA_CHANGES_2026-07-02.md`.

**2026-06-28 sync result:** token refresh (all 7 snapshots) — 2 new tokens `--surface-primary-alt-desktop-badge` + `--border-primary-alt-desktop-badge` (Coral Red Alt badge). Component changes: Desktop Badges gained `Coral Red (Alt)` 11th colour (30→33 variants) + padding update (8→6 outer, 4 icon-adjacent) — **applied to `Badge.tsx` + stories**. Data Entry gained `Search Input Desktop Error` variant (already built). Desktop Dropdowns gained 2 new filter-search variants (already built). Large Modal gained Dual Column Form (already built). `tokens:check` clean.

**2026-06-22 sync result:** token fingerprint of all 5 variable collections + text/effect styles found **no drift** (the prior `alias`/`mapped-colors`/`mapped-sizes` fingerprint mismatch was a trailing-newline artifact only). The catalog diff vs the stale 2026-06-03 baseline was entirely **already-reconciled** code work (renames, axis/childCount changes, the Bulk Actions Toolbar swap, `Icon/Open` + `Icon/Return` — all present in code); no new action items. Baseline files refreshed so future syncs diff cleanly.

The 2026-06-03 run also produced `docs/figma-changes/FIGMA_CHANGES_2026-06-03.md` and a `## Pending Figma sync (2026-06-03)` section in PLAN.md.

Token snapshots (`alias.json`, `brand.json`, `effect-styles.json`, `mapped-*.json`, `text-styles.json`) are owned by the token pipeline, not the skill — the skill reads them but never overwrites them.

### ✅ Token re-sync + Tooltip reconciliation (2026-06-24)

Re-pulled all 5 variable collections via the bridge (`brand`/`alias`/`mapped-colors`/`mapped-type`/`mapped-sizes` snapshots) + `pnpm tokens:generate`; `tokens:check` clean. The change (diffed regenerated dist vs a backup) was a **Tooltip surface-token restructure**:
- **Removed** `--surface-neutral-tooltip` (was white/black).
- **Added** `--surface-neutral-popover`, `--surface-neutral-flyout`, `--surface-neutral-coachmark` (white/black) + **`--surface-secondary-tooltip`** (secondary-900 light / secondary-50 dark — a *dark* tooltip surface).
- `--text-on-color-label-idle` shifted (neutral-200→300 light, default→400 dark) — auto-applies to consumers.

**Tooltip component reconciled** to the updated Figma `1178:15588` ("Tooltips & Popovers"): per-variant surface/border/text — **Small + Standard are now DARK** (`--surface-secondary-tooltip` + `--border-secondary-default` + `--text-on-color-label`); **Coachmark / Flyout / Popover** use their white `--surface-neutral-{coachmark,flyout,popover}` tokens + neutral border. The **`advanced` variant was renamed `popover`** (type + story). Caret/divider follow the per-variant surface/border. typecheck clean; dist rebuilt; verified in Storybook (`Molecules/Tooltip`).

### ✅ Footer Frame / Modal scroll-shadow + Large Modal forms + Popover toggle (2026-06-24)

- **Footer Frame** (`6448:32008`): Figma removed the **top outline** from all variants → `FooterFrame.tsx` now renders no `borderTop`.
- **ModalShell scroll affordance** (replaces the footer outline; all 4 modal templates): when the body overflows, a **subtle `--shadow-neutral-1`** appears **under the header** once scrolled from the top, and **above the footer** while more content sits below — **dynamic** (each hides at its scroll end), tracked via a scroll/`ResizeObserver` handler on the body. Documented in the 4 modal Figma descriptions + ModalShell JSDoc.
- **Large Modal** (`228:11413`): variants renamed/added — `single-column-form` (was Add & Edit; short forms, <4 nested accordions) + **`dual-column-form`** (NEW; long forms, 2+ groups → two Input-Section columns split by a **full-height vertical dotted demarcator** that reaches the footer even when the columns are collapsed — row uses `flex:1 0 auto` + `align-items:stretch`, border `dotted`) + `drawer-read` + `centered`. Stories `Single Column Form` / `Dual Column Form` / `Drawer (Read)` / `Centered`. Behaviour-spec written to Figma `228:11413`. Verified both collapsed (image-1: demarcator fills body to footer) + expanded (image-2: Group Name + full-width fields) match Figma. *(Variant name = "Dual", per user.)*
- **SelectionControl**: new **`fullWidth`** prop (Figma `szH: FILL`) — stretches the row, label fills, control flush right. Fixes the **Popover** Configuration Control's stray right-padding (the switch is now full-width 248, toggle flush right, `gapAfterToggle: 0`); default stays hug (ContentPrimitives trailing toggles unaffected).

typecheck clean; dist rebuilt; all verified in Storybook (Dual Column Form layout, popover toggle flush, scroll shadows toggling at top/bottom). **⚠ Figma library dirty (Modal Dialog + Page + Large Modal + 4 scroll-doc description writes) → publish.**

### ✅ Session 2026-06-25 — Page/SideBar/TopBar fixes + Checkbox/Radio scroll bug + NotificationBanner padding + EmptyState illustrations (asset files)

All verified in Storybook (port 6107) + DOM measurement; typecheck + dist rebuild clean each time. **No Figma writes this session (read-only)** — nothing new to publish from this work.

- **Large Modal — dual-column demarcator** (`LargeModal.tsx`): vertical demarcator changed `dotted` → **solid** `--stroke-xs`/`--border-neutral-default` (per user). (The Figma `228:11413` description already said "vertical demarcator" — no "dotted" — so nothing rewritten there.)
- **Page (`9626:14490`) rebuilt to match Figma** (`Page.tsx`): re-enumerated both variants. Body = Figma `Data/Config Table Page Body` pad **[24,24,0,24]** gap 24, **fills the viewport and never scrolls itself** (`overflow:hidden`); the data-table **Container** gets pad-bottom **16** + the **TableContainer fills it (`fillHeight`) so the table scrolls internally with Pagination always visible**; config variant = fixed `Sidetab` + internally-scrolling Configuration column. Sidebar passes Figma's collapsed nav (8+5 icons). **New `fillHeight` prop on `Table` + `TableContainer`** (flex:1/minHeight:0 root + body region `flex:1;minHeight:0;overflow:auto`) — reusable for any fixed-viewport table shell.
- **Storybook fullscreen padding** (`apps/storybook/.storybook/preview.tsx`): the global surface decorator now drops its 16px padding when `parameters.layout === 'fullscreen'` (Page/Top Bar/SideBar render edge-to-edge; component stories keep breathing room).
- **Checkbox + RadioButton phantom-scroll bug FIXED** (`Checkbox.tsx`, `RadioButton.tsx`): the visually-hidden `position:absolute` `<input>` had **no positioned ancestor** → `offsetParent` fell back to `<body>`, so inside a scroll container (e.g. the table body) the inputs for scrolled-out rows escaped the clip and inflated `document.scrollHeight` (caused the Page's "phantom" document scroll). Fix: added **`position: relative`** to each control's `<label>` wrapper. **Latent bug — benefits every checkbox/radio inside any scroll container** (tables, menus, forms).
- **SideBar (`3714:35743`)** (`SideBar.tsx`): (1) header is now a fixed **64px** box-border so its bottom border lines up exactly with the Top Bar's (was 65px → 1px "disjointed" line). (2) Added the Figma **right border** (`--stroke-xs`/`--border-neutral-default`, INSIDE) — both variants have a right-only stroke; the component was missing it (the standalone story's `Frame` border had masked the gap; that frame border was removed).
- **Top Bar story** (`TopBar.stories.tsx`): removed the `FullBleed` `margin:-16` wrapper (it cancelled the *old* decorator padding; after the fullscreen-padding fix it mis-shifted the bar). Bar now renders full-bleed (`left:0,width:100%`). TopBar component already matched Figma `7519:27364` (64h, pad 12/24, 1px bottom border).
- **Notification Banner (`3811:65015`)** (`NotificationBanner.tsx`): filled-variant root padding **`12px 16px` → uniform `16px`** (`--padding-16px`); subtle stays 0. (Designer "mostly padding" update.)
- **Empty State (`7339:29150`) — real illustrations** (`EmptyState.tsx` + new `illustrations.ts` + `src/EmptyState/assets/*.png`): designer added **150×150 line-art illustration atoms** (`Illustrations/*`, each with its own ground shadow) replacing the Material-Symbol-over-ellipse. 13 illustrations (No Products **reuses** No items); illustration is a **constant 150×150** for both sizes (mobile/desktop only changes heading type style). Root now pad 16 + gap 20 (Figma frame). **Shipped as real asset files OUT of the JS bundle** (user choice): PNGs (600→300px) in `src/EmptyState/assets/`, referenced via `new URL('./assets/<x>.png', import.meta.url).href` (Vite resolves from source; esbuild leaves the expression intact). **Build step copies `src/EmptyState/assets` → `dist/assets`** (added to `@leta/components` `package.json` build script). dist/index.js back to **675 KB** (was 1.45 MB when base64-inlined). **WebP shipped** — installed `cwebp` (`brew install webp`, v1.6.0) and generated `.webp` siblings (`cwebp -q 80`, **569 KB → 67 KB total, ~88% smaller**); `illustrations.ts` exports a parallel `EMPTY_STATE_ILLUSTRATIONS_WEBP` map and EmptyState renders `<picture><source type="image/webp">…<img …PNG fallback></picture>` (verified the browser picks `*.webp` in Storybook). Both formats live in `src/EmptyState/assets/` (+ copied to `dist/assets`). To regenerate after a PNG update: re-run `cwebp -q 80` per file. Source PNGs (600px originals) live at `~/Desktop/LETA.AI/Leta Illustrations, Images and SVGs/Illustrations/`.

### ✅ Session 2026-06-27 — Figma component updates batch (icons + Footer Frame + Search/Map/Dropdown reconciliation)

All verified in Storybook (6107) + DOM; typecheck + dist rebuild + `tokens:check` clean. **⚠ Figma library dirtied this session by the icon `preferredValues` sweep → user must publish.**

- **New icon `Edit-Off`** (Figma `Icon/Edit-Off` `10674:19965` + `Icon/Edit-Off-Outline` `10674:19964`, glyphs `edit-off-rounded` / `edit-off-outline-rounded` → svg-500 `edit_off-fill.svg`/`edit_off.svg`). Added both to `packages/icons/snapshots/inventory.json` (now 360 components) + `pnpm registry` (now **257 semantic names**, `Edit-Off` has fill+outline) + rebuilt icons dist → `<Icon name="Edit-Off" />` works. **Figma preferredValues sweep:** appended both icon keys to **all 50 icon instance-swap slots across 28 component sets** (`editComponentProperty`, idempotent) so designers can pick Edit-Off everywhere (same as the prior `Icon/Open` sweep). Keys: fill `4d03d2485016a6099e5bd26718f6f46e5fb7ed21`, outline `607fa3d986be15fde1edbbf7a23165037c6c5262`.
- **Footer Frame (`6448:32008`)** (`FooterFrame.tsx`): all modal variants **80/88 → uniform 72px**, padding **20 → 16**; Card stays 40/0. Story demarcator 1.5→1px (`--stroke-xs`) + docs height text fixed.
- **Search Input (`SearchInput`) — new Error state** (Figma Data Entry Desktop/Error): added `error` (border `--border-error-default` via a new `SearchField` `error` prop) + `errorMessage` (default "Enter a valid character/value") → an error row below the field (`Icon/Error` `--icons-error-default` + `--text-error-label`). New `Error` story under `Molecules/Form Controls/Search Input/Desktop`.
- **Map Search Input (`MapSearchInput`, Web Map)**: active-results list now has **1px `Divider`s between option rows** (`--stroke-xs`; Figma "section of options" demarcator, was 1.5px) + list padding `0 8 16` → **`0 10 40`** (`--padding-10px`/`--padding-40px`). Empty-results already uses the new no-results **illustration** EmptyState (no change needed beyond the EmptyState rewrite).
- **Dropdown Advanced (Desktop Menu Options `dropdown-advanced`)**: Figma flattened it off the ContentPrimitives instance (reparenting fix). **No React change** — my `dropdown-advanced` composes `ContentPrimitives type="utility"` which renders the identical flattened output (leading anchor + title + subtext + trailing chevron); verified in Map active-results + Stacked List.
- **Desktop Dropdowns (`8230:26475`) — Basic Filter** now **3 variants** (`basic-filter` / `basic-filter-search` / `basic-filter-search-empty`) and all filter footers use the real **`FooterFrame` instance**: Basic Filter family → Footer Frame **Default** (Reset + Show Results, no leading), pinned to fixed **328px** card (list flex-fills); **Filter Group / Filter Group Empty** → Footer Frame **Data Summary** (leading "{n} results" + Reset + Show Results). Replaced the old custom `FilterFooter` div. New stories `BasicFilterSearch` / `BasicFilterSearchEmpty`.

**Round 2 follow-ups (same session):**
- **dropdown-advanced DECOUPLED from ContentPrimitives** (`DesktopMenuOptions.tsx`, per user — for consistency with the Figma flatten): the `dropdown-advanced` case now renders a flat title (`text-label-m-semibold`/`--text-default-heading`) + subtext (`text-body-m-regular`/`--text-default-sub-body`) column + trailing `Chevron-Right` 20 (`--icons-neutral-default`) — pixel-identical to the old composition (verified `rgb(16,16,16)`/`rgb(79,79,79)`). Removed the `ContentPrimitives` import.
- **Basic Filter Search frame bottom outline** (`DesktopDropdowns.tsx`): the search wrapper in `basic-filter-search`/`-empty` now has pad 8 + a 1px bottom border (`--border-neutral-default`) — Figma "Search" frame `strokeSides [0,0,1,0]`.
- **Footer Frame scroll shadow** (`FooterFrame.tsx` new `scrollShadow` prop): applies a subtle upward drop shadow `box-shadow: -2px -0.8px 8px rgba(79,79,79,0.1)` (Figma effect on the dropdown footers) when the footer pins below a scrolling region. Wired on in the Desktop Dropdowns `FilterFooter` (Basic Filter + Filter Group). Mirrors the documented ModalShell scroll-affordance pattern. **Rule: this behaviour applies to EVERY Footer Frame variant EXCEPT `card`** (Card footers are flush/transparent — `scrollShadow` is a no-op there, guarded in code). Documented in the FooterFrame prop JSDoc, the `Molecules/FooterFrame` Storybook docs + a new `ScrollShadow` demo story, and the Figma `6448:32008` description (⚠ description write → library dirty, publish).
- **`useScrollShadow(containerRef)` hook** (`packages/components/src/FooterFrame/useScrollShadow.ts`, exported from `@leta/components`): companion to the `scrollShadow` prop. Returns `true` while `el.scrollTop + el.clientHeight < el.scrollHeight - 1` (content still below the fold), `false` once at the bottom or not overflowing. Re-evaluates on `scroll` events + `ResizeObserver` notifications. **Use this for dynamic surfaces** (panels, configuration cards, playground screens) where the list may or may not overflow — pass `true` directly only for always-scrollable containers (dropdowns). `ModalShell` was refactored to use this hook internally for its footer shadow (was an inline `atBottom` state). Verified in Storybook: shadow active at scroll-top → disappears on scroll-to-bottom. Stories: `Molecules/FooterFrame/Scroll Shadow (static)` + `Molecules/FooterFrame/Scroll Shadow (dynamic via useScrollShadow)`.

### ✅ Overscroll behaviour — system-wide (2026-06-28)

**Rule (3 tiers):**
- **Root page scroll** (`html`/`body`) — native browser defaults untouched (elastic bounce on iOS, standard desktop). The `Page` component already uses `overflow: hidden` on its body wrapper, so the document never scrolls.
- **Nested/internal scroll containers** → `overscrollBehavior: 'contain'` — isolates the scroll chain so the parent never scrolls and pull-to-refresh is never triggered at a list boundary.
- **Data tables + snap-scroll surfaces** → `overscrollBehavior: 'none'` — zero bounce/rubber-band; prevents distracting layout shifts when aligning tabular data or advancing a snap list.

**Applied to components (inline `overscrollBehavior` prop matching existing inline-style convention):**

*contain:* ModalShell body · SideBar nav content · Page config column · DesktopDropdowns `ScrollList` + all four filter-group panes · MapSearchInput results panel · MobileDropdown panel · DateTimePicker desktop time-list + mobile content · DateTimePicker WheelColumn is `none` (precision snap, see below).

*none:* Table body (both `maxBodyHeight` and `fillHeight` code paths, both-axes scrollX path) · DateTimePicker `WheelColumn` (`scrollSnapType: 'y mandatory'`).

**Global CSS utility** (`packages/design-tokens/src/scroll-utilities.css` → exported as `@leta/design-tokens/scroll-utilities.css`):
- `.scroll-isolated { overscroll-behavior: contain }` — for host-app HTML/CSS surfaces (playground pages, custom panels)
- `.scroll-rigid { overscroll-behavior: none }` — for app-level data grids
- Imported in `apps/storybook/.storybook/preview.tsx` (classes available in all stories).
- **Playground MUST also import it** alongside `@leta/design-tokens/css` — add `import '@leta/design-tokens/scroll-utilities.css'` to the playground entry point. Build step in `packages/design-tokens/package.json` copies `src/scroll-utilities.css → dist/scroll-utilities.css` on every `pnpm build`.

Verified: filter-group left+right panes `contain` (DOM confirmed), Table body `none` (400px / 1271px scroll, DOM confirmed).

## ⛔ Building screens — NEVER hand-roll a component (set 2026-06-29)

**Non-negotiable rule for the playground and every app screen.** Every screen in the wireframe
file is a **tree of `@leta/components` instances** — it is never raw markup. Before building ANY
screen or element:

1. **Inspect the wireframe node via the Figma Desktop Bridge** (`figma_execute` walking the node) and
   read its **component instances + `componentProperties` + auto-layout** (`layoutMode`/`itemSpacing`/
   padding). The instance tree tells you exactly which design-system component and which variant/props
   each region is. (When the bridge is down, reconnect it — do not substitute a screenshot.)
2. **Map every region to its `@leta/components` component** (Page → SideBar/TopBar/TableContainer →
   Table → Cell types → DesktopDropdowns/DateTimePicker, etc.).
3. **Compose only.** Raw `<div>`/`<button>` with token styles that *imitate* a component (a custom
   "table", a hand-built menu, a restyled chip) is a **bug**. Raw elements are allowed ONLY for
   non-component layout primitives — flex wrappers, portals, positioning shells.
4. If a region appears to have **no** matching component, **STOP and confirm with the user** before
   hand-rolling anything.

**Why this rule exists:** the first Orders/Deliveries screen was hand-rolled (custom HeaderCell/
BodyCell/OrderRow, a fake table, hand-built dropdowns) because it was built from a *screenshot
mental-model* instead of the wireframe's instance tree. Bridge-inspecting `594:59213` showed it is
literally `SideBar + TopBar + Data Table Page Body (pad [24,24,0,24]) + Content (pad-bottom 16) +
TableContainer(Default) → Table → Cell` — all already built. Always inspect instances first.

### Reading flow connections from the wireframe (set 2026-06-29)

How the designer encodes how components connect, and how to read each via the bridge:

1. **Prototype interactions (the arrows)** — the PRIMARY signal. Read `node.reactions`
   (`trigger.type` like `ON_CLICK`/`ON_HOVER`/`MOUSE_ENTER` + `action.destinationId`). These give the
   exact destination node ID for "clicking/hovering X opens Y" — machine-readable, no text parsing.
   The Scheduled section has ~26 of these (e.g. row ⋯ → overflow menu, avatar → user menu, Created →
   date picker). Resolve `destinationId` to a node to see the target component/group.
2. **Pinned annotations (the Annotate tool)** — readable via `figma_get_annotations` or `node.annotations`
   (`label`/`labelMarkdown`). The designer names a group descriptively (e.g. "User Menu Dropdown") and
   references it by name in an annotation on the trigger; read both and connect them.
3. **Sticky-note widgets** — their text is **NOT** extractable via the plugin API (`widgetSyncedState`
   returns `{}`). **Screenshot the node** (`figma_capture_screenshot <id>`) and read the image instead.

**MANDATORY before building any flow:** after reading the prototype reactions + annotations + sticky
notes, **present the inferred connections back to the user as a multiple-choice quiz / short list of
clarifying questions (AskUserQuestion) and wait for confirmation** before implementing. Do not assume a
connection's exact behavior (e.g. whether a status filter defaults to a sub-status or shows the whole
group) — verify it.

**Token-name gotcha (confirmed 2026-06-29):** there is **no `--surface-neutral-bg-primary`** token —
white surfaces use **`--surface-neutral-bg-default`** (others: `-muted`/`-raised`/`-subtle`). A
`var(--surface-neutral-bg-primary)` reference resolves to nothing → transparent (caused a dropdown
bleed-through bug). CLAUDE.md notes above that say "bg-primary" are stale; the real token is `bg-default`.
After editing any `packages/components` source, **rebuild the dist** (`pnpm --filter @leta/components build`)
or the playground (which imports the built dist) shows the old version — and **clear Vite's cache**
(`rm -rf apps/playground/node_modules/.vite`) + restart so the pre-bundled dep updates.

### ✅ Phase 3 Playground — built + deployed (superseded the section below)
`apps/playground` is no longer a stub — it's a working app (Orders/Deliveries + Map screens), live on Vercel at the `LETA` team project `leta-playground-playground`, repo `github.com/ifeanyi-99/LETA-Playground`. See the Status entry above for the full build + hardening + deployment history (including the `design-tokens` build-script fix required to get Vercel's fresh-checkout build passing). The guidance below is kept for historical record of the original scaffolding decisions (token package name, MapView-not-Mapbox, etc.), all of which were followed.

<details>
<summary>Original Phase 3 scaffolding brief (2026-06-24, kept for history)</summary>

**`apps/playground` already exists** (`@leta/playground`) with package.json deps (`@leta/components`, `@leta/design-tokens`, `@leta/icons`, **`react-router-dom@7`**, vite + plugin-react + TS) but **no source/vite.config/tsconfig/index.html yet** — build the app *inside it*, do not create `apps/LETA-Cloud/playground`. Token package is **`@leta/design-tokens`** (NOT `@leta/tokens`). Theme setup mirrors Storybook `preview.tsx`: import `@leta/design-tokens/css` + `@leta/design-tokens/text-styles.css` + **`@leta/design-tokens/scroll-utilities.css`** + `@leta/design-tokens/fonts` and set `data-theme="light"` on the root (optionally wrap in `LetaThemeProvider` from `@leta/design-tokens`). **Use the design-system `MapView` (Leaflet/OSM, no API key) for the map — do NOT add react-leaflet/Mapbox.** Reuse `SideBar` + `TopBar` (or the `Page` shell) for AppShell chrome. `zustand` not yet a dep (add if chosen). Keep Storybook untouched. **Build screens by composing design-system components — see the "NEVER hand-roll" rule above.**

</details>
