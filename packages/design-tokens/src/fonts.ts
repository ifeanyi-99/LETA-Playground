// Font loading entry: import this once at the app root to register the
// Inter Variable font that the LETA text styles reference.
//
// Usage:
//   import '@leta/design-tokens/fonts';
//
// The variable font ships with @fontsource so all weights (Light → Bold) are
// served from one woff2. No external Google Fonts request, no FOUT from
// progressive weight loading.
import '@fontsource-variable/inter';
