{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm turbo run build --filter=@leta/playground...",
  "outputDirectory": "apps/playground/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
